# 公式详细推导与解析

> **论文**：*Energy-based physics-informed neural network for frictionless contact problems under large deformation*
> 
> 本文档对论文中所有编号公式（Eq. 1–24、A.1–A.8）进行逐一解释、给出必要的推导过程，并阐明公式之间的内在联系。按论文章节顺序组织。

---

## 第2章　基于能量的PINN非线性固体力学框架

### 2.1　由势能驱动的损失函数

---

#### 公式 (1)：神经网络位移近似与本质边界条件

\[
\begin{array}{rl}
& \pmb{u}(\pmb{x}) \approx \pmb{F}(\pmb{x};\pmb{\theta}) \\
& \pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x}), \qquad \pmb{x} \in \Gamma^{\pmb{u}}
\end{array}
\tag{1}
\]

**符号说明**

- \(\pmb{u}(\pmb{x})\)：固体在空间点 \(\pmb{x}\) 处的位移向量场（待求解的真实解）。
- \(\pmb{F}(\pmb{x};\pmb{\theta})\)：神经网络定义的映射，以坐标 \(\pmb{x}\) 为输入，以位移预测为输出；\(\pmb{\theta}\) 是网络中所有可训练参数（权重与偏置）的集合。
- \(\bar{\pmb{u}}(\pmb{x})\)：在本质边界 \(\Gamma^{\mathrm{u}}\) 上规定的位移值（已知量）。
- \(\Gamma^{\mathrm{u}}\)：本质边界（Dirichlet 边界），位移在该边界上被明确约束。

**解读与推导**

公式 (1) 分两行表达了 PINN 方法的核心思想：

**第一行**说明神经网络如何替代传统插值基函数。在有限元法中，位移场由形函数插值给出：\(\pmb{u}(\pmb{x}) \approx \sum_I N_I(\pmb{x})\,\pmb{u}_I\)，其中 \(N_I\) 是形函数，\(\pmb{u}_I\) 是节点自由度。在 PINN 方法中，整个位移场由一个前馈神经网络统一近似：

\[
\pmb{u}(\pmb{x}) \approx \pmb{F}(\pmb{x};\pmb{\theta}) = W^{(L)}\sigma\bigl(W^{(L-1)}\sigma(\cdots\sigma(W^{(1)}\pmb{x}+\pmb{b}^{(1)})\cdots)+\pmb{b}^{(L-1)}\bigr)
\]

其中 \(W^{(l)}\) 和 \(\pmb{b}^{(l)}\) 分别是第 \(l\) 层的权重矩阵和偏置向量，\(\sigma(\cdot)\) 是激活函数（本文采用 \(\tanh\)）。\(\pmb{\theta} = \{W^{(1)},\pmb{b}^{(1)},\ldots,W^{(L)},\pmb{b}^{(L)}\}\) 是所有可训练参数。

**第二行**是容许位移场的约束条件：解 \(\pmb{u}(\pmb{x})\) 必须在本质边界 \(\Gamma^{\mathrm{u}}\) 上满足规定值。这与有限元法中节点位移约束的含义完全相同。

**与后续公式的联系**：公式 (1) 是整个框架的起点。满足约束条件有两种方式：
- **软边界条件**（公式 7–8）：将约束作为惩罚项加入损失函数，近似满足。
- **硬边界条件**（公式 9–10）：直接修改网络输出结构，精确满足。

训练目标是找到最优 \(\pmb{\theta}^*\)，使得 \(\pmb{F}(\pmb{x};\pmb{\theta}^*)\) 尽可能接近真实位移解，这由公式 (2) 的最小势能原理来驱动。

---

#### 公式 (2)：最小势能原理

\[
\pmb{u}(\pmb{x}) = \arg\min_{\pmb{u}(\pmb{x})} \Pi\bigl(\pmb{u}(\pmb{x})\bigr)
\tag{2}
\]

**符号说明**

- \(\arg\min_{\pmb{u}}\)：使 \(\Pi\) 取最小值的位移场 \(\pmb{u}\)。
- \(\Pi(\pmb{u}(\pmb{x}))\)：系统的总势能泛函（定义见公式 3）。

**推导背景**

最小势能原理（Principle of Minimum Potential Energy）是弹性力学的基本变分原理：**在所有满足本质边界条件的容许位移场中，真实位移场使系统总势能取最小值**。

其严格表述为：设 \(\mathcal{U}\) 是所有满足 \(\pmb{u}|_{\Gamma^{\mathrm{u}}} = \bar{\pmb{u}}\) 的容许位移场的集合，则真实解 \(\pmb{u}^*\) 满足：

\[
\Pi(\pmb{u}^*) \leq \Pi(\pmb{u}), \quad \forall\, \pmb{u} \in \mathcal{U}
\]

**从变分方程推导最小势能原理**

虚功原理（弱形式）要求：对任意虚位移 \(\delta\pmb{u}\)（在 \(\Gamma^{\mathrm{u}}\) 上为零），有：

\[
\int_\Omega \pmb{\sigma} : \delta\pmb{\varepsilon}\,\mathrm{d}\Omega = \int_\Omega \pmb{b}\cdot\delta\pmb{u}\,\mathrm{d}\Omega + \int_{\Gamma^{\mathrm{t}}} \bar{\pmb{t}}\cdot\delta\pmb{u}\,\mathrm{d}\Gamma
\]

其中 \(\pmb{\sigma}\) 是应力张量，\(\pmb{\varepsilon}\) 是应变张量，\(\pmb{b}\) 是体力。对于弹性材料，\(\pmb{\sigma} = \partial\Psi/\partial\pmb{\varepsilon}\)，因此上式等价于势能泛函 \(\Pi\) 对 \(\pmb{u}\) 的一阶变分为零：\(\delta\Pi = 0\)，即 \(\Pi\) 在真实解处取极值（且为最小值）。

**在 PINN 框架中的作用**：公式 (2) 将求解偏微分方程的问题转化为一个**无约束优化问题**——最小化势能泛函 \(\Pi\)。这正是梯度下降优化器（如 Adam）所擅长的。因此，PINN 的训练等价于在神经网络参数空间中寻找使 \(\Pi\) 最小的参数：

\[
\pmb{\theta}^* = \arg\min_{\pmb{\theta}} \Pi\bigl(\pmb{F}(\pmb{x};\pmb{\theta})\bigr)
\]

---

#### 公式 (3)：总势能的组成

\[
\Pi = E_{\mathrm{in}} - E_{\mathrm{ex}}
\tag{3}
\]

**符号说明**

- \(E_{\mathrm{in}}\)：应变能（内力做的功储存为弹性能），定义见公式 (4)。
- \(E_{\mathrm{ex}}\)：外力势能（外力在位移过程中做功的负值），定义见公式 (5)。

**推导**

总势能 \(\Pi\) 是系统内能与外力势能之差。其物理意义来自热力学：系统在平衡态时势能取极小值，这与外力做功等于应变能储存的能量守恒关系一致。

具体地：
- 弹性固体在变形过程中储存的弹性应变能为 \(E_{\mathrm{in}} = \int_\Omega \Psi(\pmb{F})\,\mathrm{d}\Omega\)（见公式 4）。
- 外力（体力 \(\pmb{b}\) 和面力 \(\bar{\pmb{t}}\)）的势能为 \(E_{\mathrm{ex}} = \int_\Omega \pmb{b}\cdot\pmb{u}\,\mathrm{d}\Omega + \int_{\Gamma^{\mathrm{t}}} \bar{\pmb{t}}\cdot\pmb{u}\,\mathrm{d}\Gamma\)（见公式 5）。

**注意**：外力势能以负号进入总势能，这是因为外力做正功时，系统的"能量优势"增大，势能减小。最小化 \(\Pi = E_{\mathrm{in}} - E_{\mathrm{ex}}\) 等价于寻找应变能与外力做功之间的平衡。

**与接触问题的扩展**：当系统涉及接触时，公式 (3) 被扩展为公式 (13)：

\[
\Pi = E_{\mathrm{in}} - E_{\mathrm{ex}} + E_{\mathrm{c}}
\]

其中 \(E_{\mathrm{c}}\) 是接触势能，作为正项加入（防穿透的排斥能增大系统势能）。

---

#### 公式 (4)：应变能

\[
E_{\mathrm{in}} = \int_{\Omega} \Psi(\pmb{F})\,\mathrm{d}\Omega
\tag{4}
\]

**符号说明**

- \(\Omega\)：固体的参考构型（未变形状态下的计算域）。
- \(\Psi(\pmb{F})\)：应变能密度函数，单位体积的弹性势能，是变形梯度 \(\pmb{F}\) 的函数。
- \(\pmb{F}\)：变形梯度张量（Deformation Gradient Tensor），定义为：

\[
\pmb{F} = \frac{\partial\pmb{x}_{\mathrm{current}}}{\partial\pmb{X}_{\mathrm{ref}}} = \pmb{I} + \frac{\partial\pmb{u}}{\partial\pmb{X}}
\]

其中 \(\pmb{I}\) 是单位张量，\(\pmb{X}\) 是参考坐标。在 2D 情形下：

\[
\pmb{F} = \begin{bmatrix} 1 + \dfrac{\partial u}{\partial x} & \dfrac{\partial u}{\partial y} \\[8pt] \dfrac{\partial v}{\partial x} & 1 + \dfrac{\partial v}{\partial y} \end{bmatrix} = \begin{bmatrix} F_{11} & F_{12} \\ F_{21} & F_{22} \end{bmatrix}
\]

**Neo-Hookean 应变能密度的推导**

本文数值算例采用 Neo-Hookean 超弹性材料。其应变能密度为：

\[
\Psi(\pmb{F}) = \frac{\lambda}{4}(J^2 - 1) - \left(\frac{\lambda}{2} + \mu\right)\ln J + \frac{\mu}{2}(I_1 - 2)
\]

其中：
- \(J = \det(\pmb{F}) = F_{11}F_{22} - F_{12}F_{21}\)：Jacobian 行列式，衡量体积变化比。\(J=1\) 表示不可压缩，\(J>1\) 表示体积膨胀，\(J<1\) 表示体积压缩。
- \(I_1 = \mathrm{tr}(\pmb{F}^{\mathrm{T}}\pmb{F}) = F_{11}^2 + F_{12}^2 + F_{21}^2 + F_{22}^2\)：变形梯度的第一不变量，衡量形状变化。
- Lamé 常数由弹性模量 \(E\) 和泊松比 \(\nu\) 给出：

\[
\lambda = \frac{\nu E}{(1+\nu)(1-2\nu)}, \qquad \mu = \frac{E}{2(1+\nu)}
\]

Neo-Hookean 模型的导出：从弹性力学出发，对于各向同性超弹性材料，应变能密度可以表示为 \(I_1,\,I_2,\,J\) 的函数，最简单的形式即为 Neo-Hookean 模型，它在小变形极限下退化为线弹性 Hooke 定律。

**数值积分**：在 PINN 框架中，积分 \(\int_\Omega\Psi(\pmb{F})\,\mathrm{d}\Omega\) 通过 Gauss 求积近似：

\[
E_{\mathrm{in}} \approx \sum_{i=1}^{N_p} \Psi\bigl(\pmb{F}(\pmb{x}_i)\bigr)\,w_i
\]

其中 \(\pmb{x}_i\) 是 Gauss 积分点坐标，\(w_i\) 是对应的积分权重（即代码中的 `y[0]`、`y[1]`）。

**与位移场的联系**：\(\pmb{F}\) 中的偏导数 \(\partial u/\partial x\) 等通过自动微分（TensorFlow 的 `GradientTape`）从神经网络输出 \(\pmb{u} = \pmb{F}(\pmb{x};\pmb{\theta})\) 对输入 \(\pmb{x}\) 求导得到，这是 PINN 方法的核心优势之一。

---

#### 公式 (5)：外力势能

\[
E_{\mathrm{ex}} = \int_{\Gamma^{\mathrm{t}}} \bar{\pmb{t}} \cdot \mathrm{d}\pmb{\Pi}
\tag{5}
\]

**符号说明**

- \(\Gamma^{\mathrm{t}}\)：自然边界（Neumann 边界），在该边界上规定面力。
- \(\bar{\pmb{t}}\)：规定的面力（Traction Force）向量，单位面积上的力。
- \(\mathrm{d}\pmb{\Pi}\)：在自然边界上的面积微元向量（即 \(\pmb{u}\,\mathrm{d}\Gamma\)）。

**更严格的表达形式**

公式 (5) 中 \(\mathrm{d}\Pi\) 的表示略有歧义，更规范的写法为：

\[
E_{\mathrm{ex}} = \int_{\Gamma^{\mathrm{t}}} \bar{\pmb{t}} \cdot \pmb{u}\,\mathrm{d}\Gamma + \int_\Omega \pmb{b} \cdot \pmb{u}\,\mathrm{d}\Omega
\]

其中第一项是面力做功（自然边界条件），第二项是体力做功（如重力）。

**自然边界条件的自动嵌入性**

能量方法的重要优势在于：**自然边界条件自动满足**，不需要额外的惩罚项。这是因为面力 \(\bar{\pmb{t}}\) 的贡献已经包含在总势能 \(\Pi\) 中。只有本质边界条件（位移约束）需要额外处理（见公式 6–10）。

对此可以用变分推导验证：对 \(\Pi\) 取变分：

\[
\delta\Pi = \delta E_{\mathrm{in}} - \delta E_{\mathrm{ex}} = \int_\Omega \pmb{\sigma}:\delta\pmb{\varepsilon}\,\mathrm{d}\Omega - \int_{\Gamma^{\mathrm{t}}} \bar{\pmb{t}}\cdot\delta\pmb{u}\,\mathrm{d}\Gamma = 0
\]

令 \(\delta\Pi = 0\) 即得弱形式（虚功方程），其中自然边界条件 \(\pmb{\sigma}\cdot\pmb{n} = \bar{\pmb{t}}\) 在边界上自动成立（通过分部积分可以验证）。

---

### 2.2　边界条件施加方法

---

#### 公式 (6)：本质边界条件

\[
\pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x}), \qquad \pmb{x} \in \Gamma^{\mathrm{u}}
\tag{6}
\]

**解读**

公式 (6) 是公式 (1) 第二行的重复表述，作为独立公式单独列出以强调其在边界条件施加方法中的核心地位。

本质边界条件（Dirichlet BC）是对位移场施加的强制约束，不同于自然边界条件（面力），它不能通过最小化势能自动满足。这是因为：神经网络 \(\pmb{F}(\pmb{x};\pmb{\theta})\) 在初始化后输出的是随机值，不会自动满足 \(\pmb{F}(\pmb{x};\pmb{\theta})|_{\Gamma^{\mathrm{u}}} = \bar{\pmb{u}}(\pmb{x})\)。

**两种施加方式**

1. **软边界条件**（Soft BC，公式 7–8）：允许小偏差，用惩罚项驱动网络输出趋近 \(\bar{\pmb{u}}\)。
2. **硬边界条件**（Hard BC，公式 9–10）：修改网络结构，使输出在 \(\Gamma^{\mathrm{u}}\) 上精确等于 \(\bar{\pmb{u}}\)。

---

#### 公式 (7)：软边界条件惩罚泛函

\[
\Pi_{\mathrm{EBC}} = \int_{\Gamma^{\mathrm{u}}} \|\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\|_2^2\,\mathrm{d}\Gamma
= \int_{\Gamma^{\mathrm{u}}} \bigl[\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\bigr] \cdot \bigl[\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\bigr]\,\mathrm{d}\Gamma
\tag{7}
\]

**符号说明**

- \(\Pi_{\mathrm{EBC}}\)：本质边界条件（Essential Boundary Condition，EBC）的惩罚泛函，度量预测位移与规定位移之间的偏差。
- \(\|\cdot\|_2^2\)：向量的 2-范数平方，即欧氏距离的平方，等于向量与自身的点积。

**推导**

公式 (7) 本质上是一个**最小二乘误差泛函**。定义边界残差 \(\pmb{r}(\pmb{x}) = \pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\)，则：

\[
\Pi_{\mathrm{EBC}} = \int_{\Gamma^{\mathrm{u}}} \pmb{r} \cdot \pmb{r}\,\mathrm{d}\Gamma = \int_{\Gamma^{\mathrm{u}}} |\pmb{r}|^2\,\mathrm{d}\Gamma \geq 0
\]

当且仅当 \(\pmb{r}(\pmb{x}) = \pmb{0}\)（即 \(\pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x})\) 在 \(\Gamma^{\mathrm{u}}\) 上处处成立）时，\(\Pi_{\mathrm{EBC}} = 0\)。这保证了只有精确满足边界条件时 \(\Pi_{\mathrm{EBC}}\) 才达到最小值。

**数值离散形式**

在有限个边界采样点 \(\{\pmb{x}_j\}_{j=1}^{N_b}\) 上，面积分近似为：

\[
\Pi_{\mathrm{EBC}} \approx \sum_{j=1}^{N_b} |\pmb{u}(\pmb{x}_j) - \bar{\pmb{u}}(\pmb{x}_j)|^2\,\Delta\Gamma_j
\quad \text{（或均值形式：}\frac{1}{N_b}\sum_{j=1}^{N_b}|\pmb{u}(\pmb{x}_j) - \bar{\pmb{u}}(\pmb{x}_j)|^2\text{）}
\]

**与公式 (8) 的联系**：\(\Pi_{\mathrm{EBC}}\) 单独是无量纲的偏差度量，必须乘以惩罚因子 \(\kappa\) 才能与势能 \(\Pi\) 量级匹配，从而形成公式 (8) 的修正势能 \(\Pi^*\)。

---

#### 公式 (8)：含惩罚项的修正势能泛函

\[
\Pi^{*} = \Pi + \kappa\,\Pi_{\mathrm{EBC}}
\tag{8}
\]

**符号说明**

- \(\Pi^*\)：修正势能泛函，在原势能 \(\Pi\) 基础上加入边界条件惩罚项。
- \(\kappa\)：惩罚因子（正实数），控制边界条件满足程度的权重。

**理论分析：\(\kappa\) 的作用**

公式 (8) 是**惩罚方法**（Penalty Method）的标准形式。对其进行以下分析：

1. **当 \(\kappa = 0\)** 时，\(\Pi^* = \Pi\)，边界条件完全被忽略，训练仅最小化应变能，所得位移场不满足任何边界条件。

2. **当 \(\kappa \to \infty\)** 时，\(\Pi_{\mathrm{EBC}} \to 0\)（因为任何有限偏差都会导致 \(\Pi^*\to\infty\)），从而精确满足边界条件，但会导致优化问题的**病态条件数**（条件数 \(\sim \kappa/E\)），使梯度下降收敛困难。

3. **实际选取**：\(\kappa\) 应远大于应变能量级（使边界条件得到足够权重），但不能过大（避免数值不稳定）。论文中取 \(\kappa = 10^4\)。

**从 Lagrange 乘子法的角度理解**

精确满足本质边界条件的变分问题为（带约束优化）：

\[
\min_{\pmb{u}} \Pi(\pmb{u}), \quad \text{s.t. } \pmb{u}|_{\Gamma^{\mathrm{u}}} = \bar{\pmb{u}}
\]

对约束引入 Lagrange 乘子 \(\pmb{\lambda}(\pmb{x})\) 可得增广泛函：

\[
\Pi_{\mathrm{aug}} = \Pi + \int_{\Gamma^{\mathrm{u}}} \pmb{\lambda}\cdot(\pmb{u} - \bar{\pmb{u}})\,\mathrm{d}\Gamma
\]

对 \(\pmb{\lambda}\) 进一步近似为 \(\pmb{\lambda} \approx \kappa(\pmb{u} - \bar{\pmb{u}})\)（线性化），即得公式 (8)。

**与公式 (21) 的联系**：在处理时变（渐进加载）边界条件时，\(\Pi_{\mathrm{EBC}}\) 被推广为公式 (21) 中的时变形式 \(\Pi_{\mathrm{EBC}}(t)\)，允许边界条件随训练迭代数逐步施加。

---

#### 公式 (9)：硬边界条件施加（距离函数法）

\[
\pmb{u}(\pmb{x}) = \pmb{F}(\pmb{x}) \odot \pmb{g}(\pmb{x}) + \bar{\pmb{u}}(\pmb{x})
\tag{9}
\]

**符号说明**

- \(\odot\)：逐元素（Hadamard）积，两个同维向量对应位置元素相乘。
- \(\pmb{F}(\pmb{x})\)：神经网络的原始输出（未经边界条件修正）。
- \(\pmb{g}(\pmb{x})\)：距离函数（见公式 10），在本质边界 \(\Gamma^{\mathrm{u}}\) 上取零值，在域内为正值。
- \(\bar{\pmb{u}}(\pmb{x})\)：规定的边界位移（也可以在整个域内给出，通常取为边界值的某种延伸）。

**精确满足边界条件的证明**

公式 (9) 的核心性质：当 \(\pmb{x} \in \Gamma^{\mathrm{u}}\) 时，\(\pmb{g}(\pmb{x}) = \pmb{0}\)（见公式 10），故：

\[
\pmb{u}(\pmb{x})\big|_{\pmb{x}\in\Gamma^{\mathrm{u}}} = \pmb{F}(\pmb{x}) \odot \pmb{0} + \bar{\pmb{u}}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x})
\]

这表明，无论神经网络 \(\pmb{F}(\pmb{x};\pmb{\theta})\) 输出什么值，构造出的位移场 \(\pmb{u}(\pmb{x})\) 都**自动精确满足**本质边界条件。这是硬边界条件方法的根本优势。

**与软边界条件（公式 7–8）的对比**

| 特性 | 软边界条件（Eq. 7–8） | 硬边界条件（Eq. 9–10） |
|:---|:---|:---|
| 边界条件满足程度 | 近似（残差随 \(\kappa\) 增大而减小） | 精确 |
| 损失函数复杂度 | 需要额外的 \(\Pi_{\mathrm{EBC}}\) 项 | 无需额外惩罚项 |
| 几何适用性 | 任意复杂几何 | 简单几何（需显式 \(\pmb{g}\)）|
| 超参数需求 | 需调整 \(\kappa\) | 无需 |

**实现方式**

在论文代码中，对于沿坐标轴对齐的直线边界（如 \(y = y_0\)），距离函数简化为 \(g(x,y) = y - y_0\)，于是：

\[
u(x,y) = f(x,y)\cdot(y - y_0) + \bar{u}
\]

当 \(y = y_0\) 时自动得 \(u = \bar{u}\)。

---

#### 公式 (10)：距离函数

\[
\pmb{g}(\pmb{x}) = \min_{\pmb{y}\in\Gamma^{\pmb{u}}} \|\pmb{x} - \pmb{y}\|_2^2
= \min_{\pmb{y}\in\Gamma^{\pmb{u}}} \bigl[(\pmb{x}-\pmb{y})\cdot(\pmb{x}-\pmb{y})\bigr]
\tag{10}
\]

**符号说明**

- \(\pmb{g}(\pmb{x})\)：点 \(\pmb{x}\) 到本质边界 \(\Gamma^{\mathrm{u}}\) 的**最短距离的平方**（注意：原文使用 \(\|\cdot\|_2^2\) 即距离的平方，而非距离本身）。
- \(\pmb{y}\)：本质边界 \(\Gamma^{\mathrm{u}}\) 上的任意点（积分变量）。

**数学性质**

距离函数 \(\pmb{g}(\pmb{x})\) 具有以下关键性质：

1. **边界处为零**：\(\forall\, \pmb{x} \in \Gamma^{\mathrm{u}}: \pmb{g}(\pmb{x}) = 0\)，因为此时 \(\min_{\pmb{y}\in\Gamma^{\mathrm{u}}} \|\pmb{x}-\pmb{y}\|_2^2 = 0\)（取 \(\pmb{y} = \pmb{x}\)）。
2. **域内为正**：\(\forall\, \pmb{x} \in \Omega \setminus \Gamma^{\mathrm{u}}: \pmb{g}(\pmb{x}) > 0\)。
3. **光滑性**：在域内光滑，确保位移场 \(\pmb{u}(\pmb{x})\) 在域内具有足够的可微性，用于计算变形梯度。

**简单几何情形下的显式表达**

| 边界类型 | 距离函数 \(g(\pmb{x})\) |
|:---|:---|
| 直线 \(x = 0\) | \(g = x\)（或 \(x^2\)） |
| 直线 \(y = y_0\) | \(g = (y - y_0)^2\) |
| 两侧边界 \(x=0\) 和 \(x=L\) | \(g = x(L-x)\) |
| 圆形边界 \(x^2+y^2 = R^2\) | \(g = (R^2 - x^2 - y^2)^2\) |

**与公式 (9) 的联系**：距离函数是硬边界条件的关键构件。不同于有限元的形函数构造，PINN 中的距离函数可以对复杂边界给出统一的代数表达。对于过于复杂的几何形状，可使用神经网络本身来近似 \(\pmb{g}(\pmb{x})\)（即"距离网络"）。

---

### 2.3　神经网络训练的伪动力学特性

---

#### 公式 (11)：伪速度

\[
\frac{\partial\pmb{u}}{\partial t} \approx -\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta})^{\mathrm{T}}}{\partial\pmb{\theta}} \frac{\partial\Pi}{\partial\pmb{\theta}}
\tag{11}
\]

**符号说明**

- \(\partial\pmb{u}/\partial t\)：位移场关于训练步数 \(t\)（伪时间）的变化率，称为**伪速度**。
- \(\partial\pmb{F}(\pmb{x};\pmb{\theta})^{\mathrm{T}}/\partial\pmb{\theta}\)：网络输出对参数的 Jacobian 矩阵（转置形式），维度为 \(N_u \times N_\theta\)（\(N_u\) 是位移自由度数，\(N_\theta\) 是参数数量）。
- \(\partial\Pi/\partial\pmb{\theta}\)：总势能对网络参数的梯度，维度为 \(N_\theta \times 1\)。

**推导（完整推导在附录 A 中给出，这里给出核心思路）**

在普通梯度下降（VGD）中，参数更新规则为：

\[
\pmb{\theta}^{t+1} = \pmb{\theta}^t - \eta\frac{\partial\Pi}{\partial\pmb{\theta}}\bigg|_{\pmb{\theta}^t}
\]

位移场的变化为：

\[
\Delta\pmb{u}^{t+1} = \pmb{F}(\pmb{x};\pmb{\theta}^{t+1}) - \pmb{F}(\pmb{x};\pmb{\theta}^t)
\approx \frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\Delta\pmb{\theta}^{t+1}
= -\eta\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

对学习率 \(\eta\to 0\) 取连续极限，得到伪速度：

\[
\frac{\partial\pmb{u}}{\partial t} = \lim_{\eta\to 0}\frac{\Delta\pmb{u}^{t+1}}{\eta} = -\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}\bigg|_{\pmb{\theta}^t}
\]

即公式 (11)。

**物理意义**

伪速度 \(\partial\pmb{u}/\partial t\) 描述了在 PINN 训练过程中，位移场随训练迭代的演化速率。这类似于在连续力学中，材料在时间上的运动速度，但这里的"时间"是离散训练步数（而非真实物理时间）。

训练过程中：
1. 随着 \(\Pi\) 减小（能量耗散），伪速度方向指向降低势能的方向。
2. 这等价于系统在外力作用下从初始状态"动态"演化到平衡态。

这种**伪动力学**特性使 PINN 训练可以自然地处理大变形路径依赖问题，以及通过选取合适学习率 \(\eta\) 来控制每步位移增量（避免接触面穿透，见公式 12）。

---

#### 公式 (12)：位移增量

\[
\Delta\pmb{u} \approx \eta\frac{\partial\pmb{u}}{\partial t}
\tag{12}
\]

**推导**

由公式 (11) 的推导过程可知，将连续形式（公式 11）乘以学习率 \(\eta\)，即得每步的实际位移增量：

\[
\Delta\pmb{u}^{t+1} \approx -\eta\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}} = \eta\frac{\partial\pmb{u}}{\partial t}
\]

因此，公式 (12) 是公式 (11) 乘以 \(\eta\) 的直接结果。

**实际意义**

公式 (12) 建立了学习率 \(\eta\)、伪速度 \(\partial\pmb{u}/\partial t\) 与每步位移增量 \(\Delta\pmb{u}\) 之间的定量关系。这有两个重要应用：

1. **学习率选择**：如果已知允许的最大位移增量（如接触面间距的一半），可以反推所需的学习率上限。

2. **接触穿透防止**：当两接触面距离为 \(d\) 时，要求 \(|\Delta\pmb{u}| < d\)，即：

\[
\eta < \frac{d}{|\partial\pmb{u}/\partial t|}
\]

这为论文中选取学习率 \(\eta = 10^{-5}\) 提供了理论依据。

**与附录 A 的联系**：公式 (11) 和 (12) 的完整推导在附录 A 中展开，涉及公式 (A.1)–(A.7)。

---

## 第3章　无摩擦接触势能与数值实现

### 3.1　无摩擦表面接触势能

---

#### 公式 (13)：含接触能的总势能

\[
\Pi = E_{\mathrm{in}} - E_{\mathrm{ex}} + E_{\mathrm{c}}
\tag{13}
\]

**符号说明**

- \(E_{\mathrm{c}}\)：表面接触势能，接触面之间排斥作用的宏观等效能量（见公式 14）。
- 其余符号同公式 (3)。

**从公式 (3) 到公式 (13) 的扩展**

公式 (3) 是单体弹性系统的势能表达。当考虑两固体之间的接触时，需要在势能中加入接触项。

**微观物理背景**：Lennard-Jones (LJ) 势 \(\phi_{\mathrm{LJ}}(r) = 4\varepsilon\left[(\sigma/r)^{12} - (\sigma/r)^6\right]\) 描述了两个原子之间的相互作用：
- 短距排斥力（\(\sim r^{-12}\)）：防止原子穿透
- 长距吸引力（\(\sim r^{-6}\)）：van der Waals 吸引

将此思想推广到宏观接触面：用接触势能 \(E_{\mathrm{c}}\)（以排斥为主，见公式 14–15）代替 KKT 条件（不等式约束），实现接触防穿透。

**\(E_{\mathrm{c}}\) 为正号的物理解释**：接触排斥作用会增大系统的能量（阻止接触面相互穿透），因此以正号加入总势能：

\[
\Pi = \underbrace{E_{\mathrm{in}}}_{\text{变形储能}} - \underbrace{E_{\mathrm{ex}}}_{\text{外力做功}} + \underbrace{E_{\mathrm{c}}}_{\text{接触排斥能}}
\]

当两接触面趋近时 \(E_{\mathrm{c}}\to +\infty\)，自动阻止穿透。

**与公式 (2) 的联系**：将公式 (13) 代入公式 (2)，则接触问题的求解等价于：

\[
\pmb{u}^* = \arg\min_{\pmb{u}} \bigl(E_{\mathrm{in}} - E_{\mathrm{ex}} + E_{\mathrm{c}}\bigr)
\]

这是一个无约束优化问题，避免了传统接触方法中不等式约束的处理难题。

---

#### 公式 (14)：表面接触势能的面积分形式

\[
E_{\mathrm{c}} = \int_{\Gamma_1}\int_{\Gamma_2} \beta_1\beta_2\,\phi(r)\,\mathrm{d}\Gamma_1\,\mathrm{d}\Gamma_2
\tag{14}
\]

**符号说明**

- \(\Gamma_1, \Gamma_2\)：两个接触面（体 1 和体 2 的潜在接触边界）。
- \(\beta_1, \beta_2\)：表面密度（单位面积上的分子/原子数密度），类比于分子动力学中的原子密度。
- \(\phi(r)\)：接触势函数（见公式 15），是接触面间距离 \(r\) 的函数。
- \(r\)：接触面 \(\Gamma_1\) 上某点与 \(\Gamma_2\) 上某点之间的距离（见公式 18–19）。
- \(\mathrm{d}\Gamma_1, \mathrm{d}\Gamma_2\)：两接触面上的面积微元。

**物理推导**

将接触势 \(E_{\mathrm{c}}\) 理解为在两接触面上所有点对之间势能 \(\beta_1\beta_2\phi(r)\) 的双重积分。这等价于分子动力学中计算两个原子集合之间总相互作用能的连续极限：

\[
E_{\mathrm{c}} = \sum_{i \in \Gamma_1} \sum_{j \in \Gamma_2} \phi(r_{ij}) \xrightarrow{\text{连续极限}} \int_{\Gamma_1}\int_{\Gamma_2} \rho_1(\pmb{x})\rho_2(\pmb{y})\phi(|\pmb{x}-\pmb{y}|)\,\mathrm{d}\Gamma_1\,\mathrm{d}\Gamma_2
\]

其中 \(\rho_1, \rho_2\) 是连续表面密度，对应公式中的 \(\beta_1, \beta_2\)。

**数值离散化（公式 14 到公式 20 的过渡）**

将面积分用离散求和近似：设 \(\Gamma_1\) 上有 \(n\) 个采样点，\(\Gamma_2\) 上有 \(m\) 个采样点，则：

\[
E_{\mathrm{c}} \approx \beta_1\beta_2 \sum_{i=1}^n\sum_{j=1}^m \phi(r_{ij})\,\Delta\Gamma_{1,i}\,\Delta\Gamma_{2,j}
\]

这正是代码中通过外积矩阵（公式 19）和对矩阵求和实现的。

---

#### 公式 (15)：指数型接触势函数

\[
\phi(r) = \phi_0\exp\!\left(-\frac{r}{r_0}\right)
\tag{15}
\]

**符号说明**

- \(\phi_0\)：势能幅值（预设的势能常数），控制接触排斥力的大小，类似于惩罚方法中的惩罚因子。
- \(r_0\)：衰减长度（特征距离），控制势能随距离衰减的速率；也决定了接触采样点的间距。
- \(r\)：接触点对之间的距离（见公式 18）。

**与 Lennard-Jones 势的对比**

| 势函数 | 表达式 | 单调性 | 适用范围 |
|:---|:---|:---|:---|
| Lennard-Jones 势 | \(4\varepsilon[(\sigma/r)^{12}-(\sigma/r)^6]\) | 非单调（先吸后斥） | 原子/分子尺度 |
| 指数型接触势（本文） | \(\phi_0\exp(-r/r_0)\) | 单调递减 | 宏观接触（纯排斥） |

LJ 势在 \(r\) 很小时表现为强排斥，在中等距离有吸引力，在大距离趋于零。在宏观接触问题中，两个固体之间不存在吸引力（只有接触时的排斥），因此用单调递减的指数型势函数代替 LJ 势是合理的简化。

**函数性质**

1. **单调递减**：\(d\phi/dr = -(\phi_0/r_0)\exp(-r/r_0) < 0\)，即随距离增大势能减小。
2. **\(r\to 0\) 时趋于 \(\phi_0\)**：\(\phi(0) = \phi_0\)（有限值，非奇异），避免了 LJ 势短距奇异性导致的数值问题。
3. **\(r\to\infty\) 时趋于 0**：\(\phi(\infty) = 0\)，远距接触面无相互作用。
4. **特征长度 \(r_0\)**：当 \(r = r_0\) 时，\(\phi(r_0) = \phi_0/e \approx 0.368\phi_0\)，即势能下降到幅值的 \(1/e\)。

**参数 \(\phi_0\) 和 \(r_0\) 对精度的影响**（论文讨论）

- **减小 \(r_0\)**：势能曲线更陡峭，接触采样点间距更小，接触精度提升，但计算量增加。
- **改变 \(\phi_0\)**：类似调整惩罚因子，结果对 \(\phi_0\) 不敏感（只要足够大以防穿透即可）；但过小会导致穿透，过大会导致训练不稳定。

**与公式 (20) 的联系**：公式 (15) 定义了单点对的势函数；公式 (20) 将其推广到所有接触点对的矩阵形式 \(\phi(r)_{n\times m}\)，用于代码实现。

---

### 3.2　数值实现

> **说明**：在原始论文中，公式编号从 (15) 跳到 (18)，说明 (16) 和 (17) 在论文中存在但未收录于本 Markdown 文件中。根据代码和上下文推断，这两个方程对应变形梯度 \(\pmb{F}\) 及 Neo-Hookean 应变能密度 \(\Psi(\pmb{F})\) 的具体形式（已在公式 4 的说明中包含）。

---

#### 公式 (18)：接触点距离矩阵

\[
r_{n\times m} = \sqrt{(\Delta\pmb{u})^2 + (\Delta\pmb{v})^2}
\tag{18}
\]

**符号说明**

- \(r_{n\times m}\)：两接触面之间所有接触点对的距离矩阵，第 \((i,j)\) 元素为 \(\Gamma_1\) 上第 \(i\) 个点与 \(\Gamma_2\) 上第 \(j\) 个点之间的距离。
- \(\Delta\pmb{u}_{n\times m}\)：两接触面水平坐标差矩阵（见公式 19）。
- \(\Delta\pmb{v}_{n\times m}\)：两接触面竖向坐标差矩阵（见公式 19）。
- 下标 \(n\times m\)：矩阵大小，\(n\) 是 \(\Gamma_1\) 上的采样点数，\(m\) 是 \(\Gamma_2\) 上的采样点数。

**推导**

在 2D 问题中，接触面 \(\Gamma_1\) 上第 \(i\) 个点的当前坐标为 \((x^{(1)}_i, y^{(1)}_i)\)，\(\Gamma_2\) 上第 \(j\) 个点的当前坐标为 \((x^{(2)}_j, y^{(2)}_j)\)。两点之间的欧氏距离为：

\[
r_{ij} = \sqrt{(x^{(1)}_i - x^{(2)}_j)^2 + (y^{(1)}_i - y^{(2)}_j)^2}
= \sqrt{(\Delta u_{ij})^2 + (\Delta v_{ij})^2}
\]

将所有点对的距离存储为矩阵，得到 \(r_{n\times m}\)。

**当前构型坐标的计算**

对于体 1 上的接触点，当前位置坐标由参考坐标加上 PINN 预测的位移得到：

\[
x^{(1)}_i = X^{(1)}_i + u^{(1)}(\pmb{X}^{(1)}_i), \quad y^{(1)}_i = Y^{(1)}_i + v^{(1)}(\pmb{X}^{(1)}_i)
\]

其中 \(u^{(1)}, v^{(1)}\) 是神经网络预测的水平和竖向位移（通过公式 1 的神经网络近似）。类似地可以得到体 2 上接触点的当前坐标。

**与公式 (14) 的联系**：公式 (18) 中的距离 \(r_{ij}\) 是计算公式 (14) 中势函数 \(\phi(r)\) 的直接输入；而 \(r_{ij}\) 本身依赖于神经网络预测的位移场，使得接触势能 \(E_{\mathrm{c}}\) 通过自动微分对网络参数 \(\pmb{\theta}\) 可微，从而可以用梯度下降法优化。

---

#### 公式 (19)：外积构造坐标差矩阵

\[
\begin{array}{r}
\Delta\pmb{u}_{n\times m} = \pmb{x}_{n\times 1}\otimes\pmb{1}_{m\times 1} - (\pmb{x}_{m\times 1}\otimes\pmb{1}_{n\times 1})^{\mathrm{T}} \\
\Delta\pmb{v}_{n\times m} = \pmb{y}_{n\times 1}\otimes\pmb{1}_{m\times 1} - (\pmb{y}_{m\times 1}\otimes\pmb{1}_{n\times 1})^{\mathrm{T}}
\end{array}
\tag{19}
\]

**符号说明**

- \(\pmb{x}_{n\times 1}\)：体 1 接触面 \(n\) 个采样点的当前水平坐标列向量。
- \(\pmb{x}_{m\times 1}\)：体 2 接触面 \(m\) 个采样点的当前水平坐标列向量。
- \(\pmb{1}_{m\times 1}\)：全 1 列向量，长度为 \(m\)。
- \(\otimes\)：外积（Outer Product），将两个列向量相乘得到矩阵：\((\pmb{a}\otimes\pmb{b})_{ij} = a_i b_j\)。

**逐步推导外积形式**

展开外积，可以验证公式 (19) 确实给出了所有点对的坐标差：

\[
\pmb{x}_{n\times 1}\otimes\pmb{1}_{m\times 1} = \begin{bmatrix} x_1^{(1)}\cdot 1 & x_1^{(1)}\cdot 1 & \cdots \\ x_2^{(1)}\cdot 1 & x_2^{(1)}\cdot 1 & \cdots \\ \vdots & \vdots & \ddots \end{bmatrix} = \begin{bmatrix} x_1^{(1)} \\ x_2^{(1)} \\ \vdots \\ x_n^{(1)} \end{bmatrix} \begin{bmatrix} 1 & 1 & \cdots & 1 \end{bmatrix}
\]

\[
(\pmb{x}_{m\times 1}\otimes\pmb{1}_{n\times 1})^{\mathrm{T}} = \begin{bmatrix} x_1^{(2)} & x_2^{(2)} & \cdots & x_m^{(2)} \\ x_1^{(2)} & x_2^{(2)} & \cdots & x_m^{(2)} \\ \vdots & \vdots & \ddots & \vdots \end{bmatrix}
\]

故差值矩阵为：

\[
\Delta\pmb{u}_{n\times m} = \begin{bmatrix}
x_1^{(1)} - x_1^{(2)} & x_1^{(1)} - x_2^{(2)} & \cdots & x_1^{(1)} - x_m^{(2)} \\
x_2^{(1)} - x_1^{(2)} & x_2^{(1)} - x_2^{(2)} & \cdots & x_2^{(1)} - x_m^{(2)} \\
\vdots & \vdots & \ddots & \vdots \\
x_n^{(1)} - x_1^{(2)} & x_n^{(1)} - x_2^{(2)} & \cdots & x_n^{(1)} - x_m^{(2)}
\end{bmatrix}
\]

第 \((i,j)\) 元素确实是 \(\Delta u_{ij} = x_i^{(1)} - x_j^{(2)}\)，验证了公式的正确性。

**TensorFlow 实现的高效性**

在 TensorFlow 2 中，利用广播（Broadcasting）机制，列向量 \((n,1)\) 与行向量 \((1,m)\) 相减自动扩展为 \((n,m)\) 矩阵，等价于外积运算，无需显式循环。代码中：

```python
dU = U1_cont * cost1 - tf.transpose(U2_cont * cost2)  # Δu_{n×m}
dV = V1_cont * cost1 - tf.transpose(V2_cont * cost2)  # Δv_{n×m}
```

这种向量化实现在 GPU 上高度并行，计算效率远高于双重 for 循环。

---

#### 公式 (20)：接触势能矩阵

\[
\phi(r)_{n\times m} = \phi_0\exp\!\left(-\frac{r_{n\times m}}{r_0}\right)
\tag{20}
\]

**推导**

将单点对势函数公式 (15) 直接应用于距离矩阵 \(r_{n\times m}\)（公式 18），即得全部接触点对的势能矩阵，第 \((i,j)\) 元素为：

\[
\phi(r_{ij}) = \phi_0\exp\!\left(-\frac{r_{ij}}{r_0}\right)
\]

**完整接触能的计算**

将公式 (20) 代入公式 (14) 的离散形式：

\[
E_{\mathrm{c}} \approx \sum_{i=1}^n\sum_{j=1}^m \phi(r_{ij})\,\Delta\Gamma_{1,i}\,\Delta\Gamma_{2,j}
= \sum_{i=1}^n\sum_{j=1}^m \phi_0\exp\!\left(-\frac{r_{ij}}{r_0}\right)\Delta\Gamma_{1,i}\,\Delta\Gamma_{2,j}
\]

在代码中，`tf.reduce_sum(phi_matrix)` 对矩阵所有元素求和，再乘以面积微元（弧长间距 \(\Delta\Gamma\)），即得 \(E_{\mathrm{c}}\) 的数值近似。

**自动微分的兼容性**

公式 (20) 及其到公式 (14) 的离散求和，整个计算图都是关于网络参数 \(\pmb{\theta}\) 可微的（通过链式法则：\(r_{ij}\) 依赖当前坐标，当前坐标依赖位移预测，位移预测依赖 \(\pmb{\theta}\)）。因此，\(\partial E_{\mathrm{c}}/\partial\pmb{\theta}\) 可以由 TensorFlow 的自动微分直接计算，无需手动推导。

---

### 3.3　松弛、渐进加载与输出缩放

---

#### 公式 (21)：渐进加载边界条件惩罚泛函

\[
\Pi_{\mathrm{EBC}}(t) = \frac{1}{n}\left\|\pmb{u}(\pmb{x}) - \frac{t}{t_{\max}}\bar{\pmb{u}}(\pmb{x})\right\|_2^2
\tag{21}
\]

**符号说明**

- \(t\)：当前训练迭代数（伪时间步数）。
- \(t_{\max}\)：当次训练的最大迭代数（一个加载步的总迭代次数）。
- \(n\)：本质边界上的采样点数。
- \(t/t_{\max}\)：当前加载进度比（从 0 到 1 线性变化）。
- \(\frac{t}{t_{\max}}\bar{\pmb{u}}(\pmb{x})\)：当前迭代步的目标位移（由零线性增长到最终规定值 \(\bar{\pmb{u}}\)）。

**从公式 (7) 到公式 (21) 的推广**

公式 (7) 是静态边界条件惩罚泛函，公式 (21) 是其时变（渐进加载）版本。具体地，将公式 (7) 中的 \(\bar{\pmb{u}}\) 替换为时变目标 \((t/t_{\max})\bar{\pmb{u}}\)，即得公式 (21)。

**物理意义与作用**

渐进加载是一种**载荷步进（load stepping）**策略，在非线性固体力学中广泛使用（类似有限元法中的增量加载）。其核心思想是：

1. **初始时（\(t=0\)）**：目标位移为 0，相当于求解无荷载自然状态（松弛状态）。
2. **训练中期（\(0 < t < t_{\max}\)）**：目标位移逐渐从 0 增加到 \(\bar{\pmb{u}}/t_{\max} \cdot t\)，位移场缓慢变化。
3. **训练结束（\(t = t_{\max}\)）**：目标位移达到最终值 \(\bar{\pmb{u}}\)，对应求解最终加载状态。

这种策略通过"慢慢加载"来防止接触面穿透：每步位移增量 \(\Delta\pmb{u} \approx \eta\partial\pmb{u}/\partial t\)（公式 12）足够小，不会超过两体之间的初始间距。

**与公式 (8) 的关系**：公式 (21) 代入公式 (8) 得到渐进加载版本的修正势能：

\[
\Pi^*(t) = \Pi + \kappa\,\Pi_{\mathrm{EBC}}(t) = \Pi + \frac{\kappa}{n}\left\|\pmb{u} - \frac{t}{t_{\max}}\bar{\pmb{u}}\right\|_2^2
\]

**与伪动力学（公式 11–12）的联系**：公式 (21) 的渐进加载设计利用了 PINN 训练的伪动力学特性：训练过程本身就是一个拟动力学演化过程（见 §2.3），只要控制每步位移增量足够小，就可以避免穿透。

---

#### 公式 (22)：带输出缩放的硬边界条件

\[
\pmb{u}(\pmb{x}) = \xi\bigl(\pmb{F}(\pmb{x})\odot\pmb{g}(\pmb{x}) + \bar{\pmb{u}}(\pmb{x})\bigr)
\tag{22}
\]

**符号说明**

- \(\xi\)：预定义的输出缩放因子（正实数）。
- 其余符号同公式 (9)。

**从公式 (9) 到公式 (22) 的扩展**

公式 (9) 是不带缩放的硬边界条件结构；公式 (22) 在整个输出上额外乘以缩放因子 \(\xi\)。

**为什么需要输出缩放**

神经网络的初始化方案（如 LeCun Normal、Xavier Normal）通常将权重初始化为使各层输出的方差约为 1，即网络输出初始分布在 \([-1, 1]\) 附近。

对于**小变形问题**（如 Hertz 接触基准，位移量级 \(\sim 10^{-3}\,\text{m}\)），网络输出 \(\pmb{F}(\pmb{x};\pmb{\theta})\) 的量级与目标位移相差 3 个数量级。在没有输出偏置时，整个缩放工作落在最后一层权重上，导致：
- 梯度量级极小，参数更新极慢
- 训练收敛困难，甚至停滞

引入 \(\xi = 10^{-3}\)（论文中 Hertz 问题的选取）后，网络只需将输出缩放到 \([-1, 1]\) 即可，最后由 \(\xi\) 自动将输出缩小到目标量级，显著加速收敛。

**对大变形问题**（如 §4 中的橡皮圈问题），位移量级与网络输出量级相当（均约为 \(O(1)\)），无需额外缩放，取 \(\xi = 1\)（即退化为公式 9）。

**缩放因子对精度的影响**：\(\xi\) 是预定义的（非可训练），不影响表达能力，仅影响训练效率。选取方式：\(\xi \approx |\bar{\pmb{u}}_{\max}|\)（规定位移的量级）。

---

### 3.4　基准测试（Hertz 接触）

---

#### 公式 (23)：Hertz 接触问题神经网络输出结构

\[
\begin{array}{l}
U = \xi\,x\,F_x(x,y), \\
V = \xi\,F_y(x,y).
\end{array}
\tag{23}
\]

**符号说明**

- \(U, V\)：水平和竖向位移的神经网络近似。
- \(F_x(x,y),\, F_y(x,y)\)：分别为预测水平和竖向位移的神经网络（FNN）输出。
- \(\xi\)：输出缩放因子（Hertz 问题取 \(\xi = 10^{-3}\)）。
- \(x\)：水平坐标，作为额外的距离函数因子。

**推导：从公式 (9)、(10)、(22) 导出公式 (23)**

Hertz 接触问题的对称边界条件：半圆柱沿对称轴 \(x = 0\) 的水平位移为零，即 \(U|_{x=0} = 0\)。

将公式 (22) 中的硬边界条件施加到 \(U\) 分量：
- 取 \(\bar{u}_U = 0\)（齐次）
- 取距离函数 \(g_U(x,y) = x\)（到边界 \(x=0\) 的距离）

则由公式 (9)：

\[
U = \xi\bigl(F_x(x,y)\cdot x + 0\bigr) = \xi\,x\,F_x(x,y)
\]

对于竖向位移 \(V\)，没有硬施加的几何边界条件，仅乘以缩放因子：

\[
V = \xi\,F_y(x,y)
\]

这与公式 (23) 完全一致。通过构造 \(U = \xi\,x\,F_x(x,y)\)，确保在 \(x=0\) 处 \(U = 0\) 精确成立，即使网络初始化为随机值也能满足对称性。

**Hertz 接触问题的分析解背景**

Hertz 接触问题描述一个弹性半圆柱在刚性平面上的压入问题。解析接触半径 \(a\) 由 Hertz 公式给出：

\[
a = \sqrt{\frac{4FR}{\pi E^*}}, \quad \frac{1}{E^*} = \frac{1-\nu_1^2}{E_1} + \frac{1-\nu_2^2}{E_2}
\]

其中 \(F\) 是总法向力，\(R\) 是圆柱半径，\(E^*\) 是等效弹性模量。在接触区内，接触压力呈椭圆分布：

\[
p(x) = p_0\sqrt{1 - \left(\frac{x}{a}\right)^2}, \quad p_0 = \frac{2F}{\pi a}
\]

PINN 框架通过最小化公式 (13) 的总势能来预测位移场，进而通过自动微分计算应力场和接触压力。

---

## 第4章　数值算例

### 4.1　橡皮熨烫问题

---

#### 公式 (24)：橡皮熨烫问题神经网络输出结构

\[
\begin{array}{l}
U_{\mathrm{c}} = (y - 3)\,u_{\mathrm{c}}(x,y) + U_{\mathrm{t}}, \\
V_{\mathrm{c}} = (y - 3)\,v_{\mathrm{c}}(x,y) + V_{\mathrm{t}}, \\
U_{\mathrm{s}} = y\,u_{\mathrm{s}}(x,y), \\
V_{\mathrm{s}} = y\,v_{\mathrm{s}}(x,y).
\end{array}
\tag{24}
\]

**符号说明**

- 下标 "c"：半圆柱（Cylinder）。
- 下标 "s"：平板（Slab）。
- \(U_{\mathrm{c}},\,V_{\mathrm{c}}\)：半圆柱的水平和竖向位移场。
- \(U_{\mathrm{s}},\,V_{\mathrm{s}}\)：平板的水平和竖向位移场。
- \(u_{\mathrm{c}}(x,y),\,v_{\mathrm{c}}(x,y)\)：预测半圆柱位移的神经网络原始输出。
- \(u_{\mathrm{s}}(x,y),\,v_{\mathrm{s}}(x,y)\)：预测平板位移的神经网络原始输出。
- \(U_{\mathrm{t}}\)：半圆柱顶面规定的水平位移（加载步中给定值）。
- \(V_{\mathrm{t}}\)：半圆柱顶面规定的竖向位移（加载步中给定值）。
- \(y = 3\)：半圆柱顶面（施加位移加载）的 \(y\) 坐标。
- \(y = 0\)：平板底面（固定约束）的 \(y\) 坐标。

**推导：从公式 (9) 到公式 (24)**

**半圆柱（Cylinder）的边界条件**

半圆柱顶面 \(y = 3\) 处施加规定位移 \((U_{\mathrm{t}},\,V_{\mathrm{t}})\)：

\[
U_{\mathrm{c}}\big|_{y=3} = U_{\mathrm{t}}, \quad V_{\mathrm{c}}\big|_{y=3} = V_{\mathrm{t}}
\]

取距离函数 \(g(y) = y - 3\)（到顶面 \(y=3\) 的有符号距离）：

\[
U_{\mathrm{c}} = u_{\mathrm{c}}(x,y)\cdot(y-3) + U_{\mathrm{t}}
\]

当 \(y = 3\)：\(U_{\mathrm{c}} = u_{\mathrm{c}}\cdot 0 + U_{\mathrm{t}} = U_{\mathrm{t}}\) ✓

类似地，\(V_{\mathrm{c}} = v_{\mathrm{c}}(x,y)\cdot(y-3) + V_{\mathrm{t}}\)，满足 \(V_{\mathrm{c}}|_{y=3} = V_{\mathrm{t}}\) ✓

**平板（Slab）的边界条件**

平板底面 \(y = 0\) 处固定，即 \(U_{\mathrm{s}}|_{y=0} = 0\)，\(V_{\mathrm{s}}|_{y=0} = 0\)：

取距离函数 \(g(y) = y\)（到底面 \(y=0\) 的距离）：

\[
U_{\mathrm{s}} = u_{\mathrm{s}}(x,y)\cdot y + 0 = y\,u_{\mathrm{s}}(x,y)
\]

当 \(y = 0\)：\(U_{\mathrm{s}} = 0\) ✓；类似地 \(V_{\mathrm{s}} = y\,v_{\mathrm{s}}(x,y)\)，\(V_{\mathrm{s}}|_{y=0} = 0\) ✓

**与公式 (9) 的统一框架**

公式 (24) 是公式 (9) 对**多个物体、多个加载方向**的具体应用：

| 位移分量 | \(\pmb{F}(\pmb{x})\) | \(\pmb{g}(\pmb{x})\) | \(\bar{\pmb{u}}(\pmb{x})\) |
|:---|:---|:---|:---|
| \(U_{\mathrm{c}}\) | \(u_{\mathrm{c}}(x,y)\) | \(y - 3\) | \(U_{\mathrm{t}}\) |
| \(V_{\mathrm{c}}\) | \(v_{\mathrm{c}}(x,y)\) | \(y - 3\) | \(V_{\mathrm{t}}\) |
| \(U_{\mathrm{s}}\) | \(u_{\mathrm{s}}(x,y)\) | \(y\) | \(0\) |
| \(V_{\mathrm{s}}\) | \(v_{\mathrm{s}}(x,y)\) | \(y\) | \(0\) |

**加载步进与公式 (21) 的联系**

在实际实现中，\(U_{\mathrm{t}}\) 和 \(V_{\mathrm{t}}\) 在每个加载步中是给定的（分步加载），对应公式 (21) 中的 \((t/t_{\max})\bar{\pmb{u}}\)。在每步训练开始时，通过软边界或硬边界方式施加当前加载量，随着加载步数增加，最终累计达到总位移 \(U_{\mathrm{t,total}}\) 和 \(V_{\mathrm{t,total}}\)。

---

## 附录 A　能量基 PINN 训练过程中的伪速度推导

附录 A 给出了公式 (11)–(12) 的完整数学推导，包含公式 (A.1)–(A.8)。

---

#### 公式 (A.1)：第 \(t\) 步的位移预测

\[
\pmb{u}^t = \pmb{F}(\pmb{x};\pmb{\theta}^t)
\tag{A.1}
\]

**解读**

这是公式 (1) 在具体训练步数 \(t\) 下的符号化表达，超标脚 \(t\) 表示第 \(t\) 次迭代时的量。\(\pmb{\theta}^t\) 是第 \(t\) 步时网络参数的当前值，\(\pmb{u}^t\) 是对应的位移场预测。

---

#### 公式 (A.2)：梯度下降参数更新规则

\[
\pmb{\theta}^{t+1} = \pmb{\theta}^t - \eta\left.\frac{\partial\Pi}{\partial\pmb{\theta}}\right|_{\pmb{\theta}^t}
\tag{A.2}
\]

**推导**

这是**普通梯度下降（VGD）算法**的更新公式。其推导来自于对势能 \(\Pi(\pmb{\theta})\) 的一阶 Taylor 展开：

\[
\Pi(\pmb{\theta}^{t+1}) \approx \Pi(\pmb{\theta}^t) + \frac{\partial\Pi}{\partial\pmb{\theta}}^{\mathrm{T}}(\pmb{\theta}^{t+1} - \pmb{\theta}^t)
\]

选择更新方向为负梯度方向（即最速下降方向）：

\[
\pmb{\theta}^{t+1} - \pmb{\theta}^t = -\eta\frac{\partial\Pi}{\partial\pmb{\theta}}\bigg|_{\pmb{\theta}^t}
\]

则：

\[
\Pi(\pmb{\theta}^{t+1}) \approx \Pi(\pmb{\theta}^t) - \eta\left\|\frac{\partial\Pi}{\partial\pmb{\theta}}\right\|^2 < \Pi(\pmb{\theta}^t)
\]

只要学习率 \(\eta > 0\) 足够小，每步迭代均可降低 \(\Pi\)，确保收敛。

**梯度的计算**：\(\partial\Pi/\partial\pmb{\theta}\) 通过反向传播（自动微分）计算，其链式法则展开为：

\[
\frac{\partial\Pi}{\partial\pmb{\theta}} = \frac{\partial\Pi}{\partial\pmb{u}}\frac{\partial\pmb{u}}{\partial\pmb{\theta}} = \frac{\partial\Pi}{\partial\pmb{u}}\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta})}{\partial\pmb{\theta}}
\]

---

#### 公式 (A.3)：参数增量

\[
\Delta\pmb{\theta}^{t+1} = \pmb{\theta}^{t+1} - \pmb{\theta}^t = -\eta\left.\frac{\partial\Pi}{\partial\pmb{\theta}}\right|_{\pmb{\theta}^t}
\tag{A.3}
\]

**推导**

直接由公式 (A.2) 两边减去 \(\pmb{\theta}^t\) 得到：

\[
\Delta\pmb{\theta}^{t+1} = \pmb{\theta}^{t+1} - \pmb{\theta}^t = -\eta\frac{\partial\Pi}{\partial\pmb{\theta}}\bigg|_{\pmb{\theta}^t}
\]

这是一个中间步骤，为后续 Taylor 展开做准备。其物理意义：参数增量方向与势能梯度方向相反（最速下降），大小由学习率 \(\eta\) 控制。

---

#### 公式 (A.4)：位移场的 Taylor 展开

\[
\pmb{u}^{t+1}(\pmb{x};\pmb{\theta}^{t+1})
\approx \pmb{F}(\pmb{x};\pmb{\theta}^t) + \frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}(\Delta\pmb{\theta}^{t+1})
\tag{A.4}
\]

**推导**

将 \(\pmb{F}(\pmb{x};\pmb{\theta}^{t+1})\) 在 \(\pmb{\theta}^t\) 处进行 Taylor 展开至一阶：

\[
\pmb{u}^{t+1} = \pmb{F}(\pmb{x};\pmb{\theta}^t + \Delta\pmb{\theta}^{t+1})
= \pmb{F}(\pmb{x};\pmb{\theta}^t) + \underbrace{\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)}{\partial\pmb{\theta}^{\mathrm{T}}}}_{\text{Jacobian矩阵}} \Delta\pmb{\theta}^{t+1} + \underbrace{O(\|\Delta\pmb{\theta}^{t+1}\|^2)}_{\text{高阶小量}}
\]

忽略高阶项 \(O(\|\Delta\pmb{\theta}^{t+1}\|^2)\)（当 \(\eta\to 0\) 时，\(\Delta\pmb{\theta} \to 0\)，高阶项可忽略），得到公式 (A.4)。

**Jacobian 矩阵的含义**：\(\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}/\partial\pmb{\theta}\) 是一个 \(N_u \times N_\theta\) 矩阵（\(N_u\) 是空间点数 × 位移维度，\(N_\theta\) 是参数数量），第 \((i,j)\) 元素为 \(\partial u_i/\partial\theta_j\)，描述参数扰动如何影响位移预测。

---

#### 公式 (A.5)：位移增量的近似

\[
\Delta\pmb{u}^{t+1} = \pmb{u}^{t+1} - \pmb{u}^t
\approx \frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}(\Delta\pmb{\theta}^{t+1})
\tag{A.5}
\]

**推导**

从公式 (A.4) 减去公式 (A.1)：

\[
\Delta\pmb{u}^{t+1} = \pmb{u}^{t+1} - \pmb{u}^t
= \pmb{F}(\pmb{x};\pmb{\theta}^t) + \frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}}\Delta\pmb{\theta}^{t+1} - \pmb{F}(\pmb{x};\pmb{\theta}^t)
= \frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\Delta\pmb{\theta}^{t+1}
\]

即得公式 (A.5)。这表明位移增量完全由参数增量 \(\Delta\pmb{\theta}^{t+1}\) 通过网络的 Jacobian 矩阵映射得到。

---

#### 公式 (A.6)：位移增量的显式表达

\[
\Delta\pmb{u}^{t+1} \approx -\eta\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}
\tag{A.6}
\]

**推导**

将公式 (A.3) 代入公式 (A.5)：

\[
\Delta\pmb{u}^{t+1} = \frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}}\Delta\pmb{\theta}^{t+1}
= \frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}}\left(-\eta\frac{\partial\Pi}{\partial\pmb{\theta}}\right)
= -\eta\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

**链式法则展开**

利用链式法则，\(\partial\Pi/\partial\pmb{\theta}\) 可进一步展开：

\[
\frac{\partial\Pi}{\partial\pmb{\theta}} = \underbrace{\frac{\partial\Pi}{\partial\pmb{u}}}_{\sim\text{内力}} \underbrace{\frac{\partial\pmb{u}}{\partial\pmb{\theta}}}_{\text{Jacobian}^{\mathrm{T}}}
\]

因此：

\[
\Delta\pmb{u}^{t+1} = -\eta\frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}} \cdot \frac{\partial\pmb{F}}{\partial\pmb{\theta}} \cdot \frac{\partial\Pi}{\partial\pmb{u}}
\]

矩阵 \(K = \frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\pmb{F}}{\partial\pmb{\theta}}\) 称为**神经正切核**（Neural Tangent Kernel, NTK），决定了训练动力学的本征结构。

---

#### 公式 (A.7)：伪速度（连续极限）

\[
\frac{\partial\pmb{u}}{\partial t} = \lim_{\eta\to 0}\frac{\Delta\pmb{u}^{t+1}}{\eta}
= -\left.\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}\right|_{\pmb{\theta}^t}
\tag{A.7}
\]

**推导**

将公式 (A.6) 两边除以 \(\eta\)，并取 \(\eta\to 0\) 的连续极限：

\[
\frac{\partial\pmb{u}}{\partial t} = \lim_{\eta\to 0}\frac{\Delta\pmb{u}^{t+1}}{\eta}
= \lim_{\eta\to 0}\frac{-\eta\frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}}{\eta}
= -\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

这正是公式 (11) 的推导来源。连续化的核心假设是 \(\eta\) 足够小，使得高阶 Taylor 展开项可以忽略，且参数更新方向近似等于负梯度方向（VGD 的情形）。

**与神经正切核（NTK）的关系**

将 \(\partial\Pi/\partial\pmb{\theta}\) 用链式法则展开：

\[
\frac{\partial\pmb{u}}{\partial t} = -\underbrace{\frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\pmb{F}}{\partial\pmb{\theta}}}_{\text{NTK: }\Theta}\frac{\partial\Pi}{\partial\pmb{u}}
\]

其中 NTK \(\Theta\) 决定了位移场在参数空间中的演化方向和速率。这是神经网络训练理论中的重要结论。

---

#### 公式 (A.8)：参数的梯度流

\[
\frac{\partial\pmb{\theta}}{\partial t} \approx -\frac{\partial\Pi}{\partial\pmb{\theta}}
\tag{A.8}
\]

**推导**

由公式 (A.3)，参数更新量为 \(\Delta\pmb{\theta}^{t+1} = -\eta\frac{\partial\Pi}{\partial\pmb{\theta}}\)。对两边除以 \(\eta\) 并取连续极限：

\[
\frac{\partial\pmb{\theta}}{\partial t} = \lim_{\eta\to 0}\frac{\Delta\pmb{\theta}^{t+1}}{\eta} = -\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

**梯度流的理论意义**

公式 (A.8) 是**梯度流（Gradient Flow）**方程，描述了参数在连续时间中的演化：参数沿损失函数的负梯度方向以"无限小步长"连续更新。这对应于以下常微分方程（ODE）：

\[
\frac{\mathrm{d}\pmb{\theta}}{\mathrm{d}t} = -\nabla_{\pmb{\theta}}\Pi(\pmb{\theta}(t))
\]

梯度流方程在优化理论中有深刻的联系：
1. **收敛性**：对于凸函数 \(\Pi\)，梯度流方程的解以指数速率收敛到全局最优。
2. **局部极小值**：对于非凸问题（如深度神经网络），梯度流可能收敛到局部极小值，这解释了为什么 VGD 可能陷入局部最优（论文附录 B 的观察）。
3. **隐式正则化**：梯度流的连续极限揭示了梯度下降的隐式正则化特性（倾向于找到特定类型的解）。

**公式 (A.7) 与 (A.8) 的对偶关系**

公式 (A.7) 描述位移场的演化（在输出空间），公式 (A.8) 描述参数的演化（在参数空间）。两者通过 Jacobian 矩阵（神经正切核）联系：

\[
\underbrace{\frac{\partial\pmb{u}}{\partial t}}_{\text{位移空间演化}} = \frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}} \underbrace{\frac{\partial\pmb{\theta}}{\partial t}}_{\text{参数空间演化}} = -\frac{\partial\pmb{F}^{\mathrm{T}}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

---

## 公式间内在联系总图

下图以因果链的形式展示了所有公式之间的逻辑关系：

```
最小势能原理 (Eq. 2)
    │
    ├──→ 势能组成 (Eq. 3) ──→ 应变能 (Eq. 4) ──→ Neo-Hookean Ψ(F)
    │           │
    │           └──→ 外力势能 (Eq. 5)
    │
    ├──→ 接触势能扩展 (Eq. 13) ──→ 接触能积分 (Eq. 14)
    │                                      │
    │                               接触势函数 (Eq. 15)
    │                                      │
    │                              数值实现 (Eq. 18–20)
    │                              ├── 距离矩阵 (Eq. 18)
    │                              ├── 外积差值 (Eq. 19)
    │                              └── 势能矩阵 (Eq. 20)
    │
    ├──→ 神经网络近似 (Eq. 1) ──→ 边界条件施加
    │                            ├── 软BC (Eq. 7–8)
    │                            │   └── 渐进加载 (Eq. 21)
    │                            └── 硬BC (Eq. 9–10)
    │                                └── 输出缩放 (Eq. 22)
    │                                └── Hertz (Eq. 23)
    │                                └── 熨烫 (Eq. 24)
    │
    └──→ 训练优化 ──→ 伪动力学 (Eq. 11–12)
                      └── 完整推导 (App. A: Eq. A.1–A.8)
```

**核心公式的逻辑链**：

1. **势能驱动优化**：公式 (2) → 公式 (3/13) → 公式 (4) → 公式 (5)，建立了将力学问题转化为无约束优化问题的数学基础。

2. **接触势能**：公式 (13) → 公式 (14) → 公式 (15) → 公式 (18–20)，从物理出发建立了避免穿透的能量惩罚机制，是本文最核心的创新。

3. **边界条件**：公式 (6) → 公式 (7–8)（软）或公式 (9–10)（硬），提供了两种处理位移约束的数学框架；公式 (21) 和 (22) 在此基础上针对接触问题做了时变和缩放两方面的改进；公式 (23–24) 是具体问题中的工程实现。

4. **训练动力学**：公式 (11–12) 揭示了 PINN 训练的伪动力学本质，为合理选取学习率提供了依据；附录 A（公式 A.1–A.8）给出了严格的数学推导，将离散迭代与连续梯度流统一在同一框架下。

