# 公式与底层计算代码对照解读

> **论文**：*Energy-based physics-informed neural network for frictionless contact problems under large deformation*  
> **代码仓库**：[https://github.com/JinshuaiBai/energy_PINN_Contact](https://github.com/JinshuaiBai/energy_PINN_Contact)  
> **格式说明**：每节以"代码在前、公式在后"的格式分段呈现，代码附可访问 GitHub 链接，公式均采用 \(\LaTeX\) 渲染。

---

## 2. Energy-based PINN for nonlinear solid mechanics

### 2.1 Loss function informed by potential energy

---

#### 公式 (1)：神经网络位移近似与本质边界条件

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/FNN.py)

```python
# FNN.py — Compression_of_Two_Rubber_Rings
import tensorflow as tf

def FNN(n_input, n_output, layers, f = 1., acti_fun='tanh', k_init='LecunNormal'):

    ### Setup the input layer of the FNN
    x = tf.keras.layers.Input(shape=(n_input))
    
    ### Setup the hidden layers of the FNN
    temp = x
    for l in layers:
        temp = tf.keras.layers.Dense(l, activation = acti_fun, kernel_initializer=k_init)(temp)
    
    ### Setup the output layers of the FNN
    y = tf.keras.layers.Dense(n_output, kernel_initializer=k_init, use_bias = False)(temp) * f

    ### Combine the input, hidden, and output layers to build up a FNN
    net = tf.keras.models.Model(inputs=x, outputs=y)

    return net
```

[`Rubber_Ironing/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py)

```python
# FNN.py — Rubber_Ironing（含 time_layer 硬边界结构）
import tensorflow as tf

class FNN:

    def __init__(self, n_in, n_out, layers, acti_fun='tanh', k_init='LecunNormal', time_layer = False, t_in = 1,
                t_variable = 0, trainable_plus_layer = False, p_variable = 0):
        self.n_in = n_in
        self.n_out = n_out
        self.layers = layers
        self.acti_fun = acti_fun
        self.k_init = k_init
        self.time_layer = time_layer
        self.t_in = t_in
        self.t_variable = t_variable
        self.trainable_pl = trainable_plus_layer
        self.p_variable = p_variable

    def net(self):
        x = tf.keras.layers.Input(shape=(self.n_in,))
        temp = x
        for l in self.layers:
            temp = tf.keras.layers.Dense(l, activation=self.acti_fun, kernel_initializer=self.k_init)(temp)
        y = tf.keras.layers.Dense(self.n_out, kernel_initializer=self.k_init, use_bias=False)(temp)

        if self.time_layer:
            t_l = time_layer(self.t_variable)
            y = t_l(y, x[..., self.t_in-1, tf.newaxis],)

        if self.trainable_pl:
            p_l = plus_trainable_layer()
            y = p_l(y)
        else:
            p_l = plus_untrainable_layer(self.p_variable)
            y = p_l(y)

        nn = tf.keras.models.Model(inputs=x, outputs=y)

        return nn

    def build(self):
        nn = self.net()
        if self.trainable_pl:
            nn = self.init_pl(nn)
        return nn
```

**公式 (1)**

\[
\begin{array}{rl}
& \pmb{u}(\pmb{x}) \approx \pmb{F}(\pmb{x};\pmb{\theta}) \\
& \pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x}), \qquad \pmb{x} \in \Gamma^{\pmb{u}}
\end{array}
\]

**解读**：`FNN` 函数（或 `FNN` 类）构建前馈神经网络 \(\pmb{F}(\pmb{x};\pmb{\theta})\)，以坐标 \(\pmb{x}\) 为输入、以位移为输出。网络可训练参数 \(\pmb{\theta}\) 对应所有 `Dense` 层的权重与偏置。在 `Rubber_Ironing/FNN.py` 中，`time_layer` 通过 \((x_{t\_in} - t\_variable)\) 乘以网络输出来硬施加本质边界条件（详见公式 9 / 24）。

---

#### 公式 (2)：最小势能原理

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — Compression_of_Two_Rubber_Rings（Adam 优化主循环，实现势能最小化）
    def fit(self):
        print('Optimizer: Adam')
        print('Initializing ...')
        self.iter = 0
        initial_weights = np.concatenate(
            [ w.flatten() for w in self.pirbn.get_weights() ])
        print('Optimizer: Adam (maxiter={})'.format(self.maxiter))
        beta1 = 0.9
        beta2 = 0.999
        learning_rate = self.learning_rate
        eps=1e-8
        x0=initial_weights
        x=x0
        m=np.zeros_like(x)
        v=np.zeros_like(x)
        for i in range(0,self.maxiter):
            loss, g = self.evaluate(x)
            m = (1 - beta1) * g + beta1 * m
            v = (1 - beta2) * (g**2) + beta2 * v  # second moment estimate.
            mhat = m / (1 - beta1**(i + 1))  # bias correction.
            vhat = v / (1 - beta2**(i + 1))
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)

        return [self.his_l1, self.his_l2, self.his_l3, self.his_l4, self.his_l5, self.his_l6, self.his_l7, self.his_l8,
                self.his_l9]
```

**公式 (2)**

\[
\pmb{u}(\pmb{x}) = \arg\min_{\pmb{u}(\pmb{x})} \Pi(\pmb{u}(\pmb{x}))
\]

**解读**：`fit()` 方法实现对势能泛函 \(\Pi\) 的最小化。每次迭代通过梯度下降（Adam 算法）更新参数 \(\pmb{\theta}\)，等价于在训练空间中寻找使 \(\Pi\) 最小的位移映射 \(\pmb{u}(\pmb{x})\)。

---

#### 公式 (3)：总势能

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — Compression_of_Two_Rubber_Rings（Loss 函数：应变能 + 外力势能）
    @tf.function
    def Loss(self, x, y):
        with tf.GradientTape() as g:
            tmp = self.pirbn(x)
            l1 = tf.reduce_sum(tmp[0] * y[0])   # 物体 1 应变能（E_in,1）
            l2 = tf.reduce_sum(tmp[1] * y[1])   # 物体 2 应变能（E_in,2）
            l3 = tf.reduce_sum(tmp[2]) * y[2]   # 接触势能（E_c 部分）
            l4 = tf.reduce_sum(tmp[3]) * y[3]
            l5 = tf.reduce_sum(tmp[4]) * y[2]
            l6 = tf.reduce_sum(tmp[5]) * y[3]
            l7 = tf.reduce_sum(tmp[6]) * y[2]
            l8 = tf.reduce_sum(tmp[7]) * y[3]
            l9 = tf.reduce_sum(tmp[8]) * 1e-3
            loss = l1 + l2 + l3 + l4 + l5 + l6 + l7 + l8 + l9
        grads = g.gradient(loss, self.pirbn.trainable_variables)
        return loss, grads, l1, l2, l3, l4, l5, l6, l7, l8, l9
```

**公式 (3)**

\[
\Pi = E_{\mathrm{in}} - E_{\mathrm{ex}}
\]

**解读**：`loss` 是总势能 \(\Pi\)，其中 `l1`、`l2` 对应各物体的应变能 \(E_{\mathrm{in}}\)（乘以积分权重 `y[0]`、`y[1]`），`l3`–`l9` 对应接触势能和外力项 \(E_{\mathrm{ex}}\)（在接触问题中扩展为公式 13）。

---

#### 公式 (4)：应变能

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py)

```python
# PINN.py — Compression_of_Two_Rubber_Rings（应变能密度积分计算）

    ### plain stress 材料参数（Lamé 常数）
    la = mu / (1 + mu) / (1 - 2 * mu) * E
    nu = 1 / (1 + mu) / 2 * E

    # 变形梯度分量
    F11_1 = (1 + U1_x)
    F22_1 = (1 + V1_y)
    F12_1 = U1_y
    F21_1 = V1_x

    # Jacobian 行列式
    J1 = F11_1 * F22_1 - F12_1 * F21_1
    # 第一不变量
    I1 = (F11_1 ** 2 + F12_1 ** 2 + F21_1 ** 2 + F22_1 ** 2)
    # Neo-Hookean 应变能密度
    e1 = 0.25 * la* (J1 ** 2 - 1) - (la / 2 + nu) * tf.math.log(J1) + 0.5 * nu * (I1 - 2)
```

[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 应变能数值积分（Gauss 求积）
    l1 = tf.reduce_sum(tmp[0] * y[0])   # ∑ Ψ(F) · w_i ≈ ∫_Ω Ψ(F) dΩ
    l2 = tf.reduce_sum(tmp[1] * y[1])
```

**公式 (4)**

\[
E_{\mathrm{in}} = \int_{\Omega} \Psi(\pmb{F}) \, \mathrm{d}\Omega
\]

**解读**：`e1`（或 `e2`）是每个 Gauss 积分点上的应变能密度 \(\Psi(\pmb{F})\)；在 `OPT.py` 中乘以 Gauss 权重 `y[0]`（即 `w1`）并求和，得到总应变能 \(E_{\mathrm{in}} \approx \sum_i \Psi(\pmb{F}(\pmb{x}_i)) w_i\)。

---

#### 公式 (5)：外力势能

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 外力势能（通过 Gauss 权重 y[0]、y[1] 嵌入应变能积分中）
# 在无接触纯力学问题中，外力项以负号并入总势能
    l1 = tf.reduce_sum(tmp[0] * y[0])  # E_in - E_ex 的综合权重项
    l2 = tf.reduce_sum(tmp[1] * y[1])
    loss = l1 + l2 + l3 + l4 + l5 + l6 + l7 + l8 + l9
```

[`Compression_of_Two_Rubber_Rings/Relax_PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/Relax_PINN.py)

```python
# Relax_PINN.py — 松弛阶段仅有应变能（无外力），对应 E_ex = 0 的情形
    @tf.function
    def Loss(self, x, y):
        with tf.GradientTape() as g:
            tmp = self.pinn(x)
            l1 = tf.reduce_sum(tmp[0] * y[0])   # 应变能 E_in
            l2 = tf.reduce_sum(tf.square(tmp[1])) + \
                tf.reduce_sum(tf.square(tmp[2]))  # 位移归零惩罚项
            loss = l1 + l2 * 1e6
        grads = g.gradient(loss, self.pinn.trainable_variables)
        return loss, grads, l1, l2
```

**公式 (5)**

\[
E_{\mathrm{ex}} = \int_{\Gamma^{\mathrm{t}}} \bar{\pmb{t}} \cdot \mathrm{d}\Pi
\]

**解读**：外力势能 \(E_{\mathrm{ex}}\) 在代码中通过积分权重 \(w_i\)（`y[0]`、`y[1]`）隐式包含在应变能 \(E_{\mathrm{in}}\) 的积分中——当无外力时（松弛阶段），`Relax_PINN.py` 中 `l2` 项强制位移趋近于零（对应自然状态），等价于 \(E_{\mathrm{ex}}=0\)。

---

### 2.2 Boundary condition imposition

---

#### 公式 (6)：本质边界条件定义

**底层计算代码**  
[`Rubber_Ironing/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py)

```python
# FNN.py — Rubber_Ironing（本质边界条件：time_layer 硬施加）
class time_layer(tf.keras.layers.Layer):
    def __init__(self, variable):
        super().__init__()
        self.unit = 1
        self.variable = variable

    def call(self, inputs, x):
        # 当 x = variable（即边界位置）时，inputs * 0 = 0，精确满足 u = 0
        return inputs * (x - self.variable)
```

**公式 (6)**

\[
\pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x}), \qquad \pmb{x} \in \Gamma^{\mathrm{u}}
\]

**解读**：`time_layer` 的输出为 \(\text{NN}(\pmb{x}) \cdot (x_{t\_in} - t\_variable)\)，当 \(x_{t\_in} = t\_variable\)（本质边界）时输出为零，精确施加齐次本质边界条件 \(\pmb{u}=\bar{\pmb{u}}\)（其中平移量由 `plus_untrainable_layer` 或 `plus_trainable_layer` 添加）。

---

#### 公式 (7)：软边界条件惩罚泛函

**底层计算代码**  
[`Rubber_Ironing/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/OPT.py)

```python
# OPT.py — Rubber_Ironing（软边界条件：均方误差惩罚项）
    @tf.function
    def Loss(self, x, y, iter):
        with tf.GradientTape() as g:
            tmp = self.pinn(x)
            l1 = tf.reduce_sum(tmp[0]*y[0])
            l2 = tf.reduce_sum(tmp[1]*y[1])
            l3 = tf.reduce_sum(tmp[2]*y[2]*y[2])
            # 软边界条件：(v(x) - v̄(t))² 的均值
            l4 = tf.reduce_mean(tf.square(tmp[3]+iter))   # Π_EBC for V（竖向位移）
            # 软边界条件：(u(x) - 0)² 的均值
            l5 = tf.reduce_mean(tf.square(tmp[4]))         # Π_EBC for U（水平位移）
            loss = l1+l2+l3+l4*1e4+l5*1e4
        grads = g.gradient(loss, self.pinn.trainable_variables)
        return loss, grads, l1, l2, l3, l4, l5
```

**公式 (7)**

\[
\Pi_{\mathrm{EBC}} = \int_{\Gamma^{\mathrm{u}}} \|\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\|_2^2 \, \mathrm{d}\Gamma
= \int_{\Gamma^{\mathrm{u}}} \bigl([\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})] \cdot [\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})]\bigr) \, \mathrm{d}\Gamma
\]

**解读**：代码中 `l4 = tf.reduce_mean(tf.square(tmp[3] + iter))` 对应 \(\Pi_{\mathrm{EBC}}^V = \frac{1}{n}\sum_i (V(\pmb{x}_i) - \bar{V}_t)^2\)，`l5 = tf.reduce_mean(tf.square(tmp[4]))` 对应 \(\Pi_{\mathrm{EBC}}^U = \frac{1}{n}\sum_i (U(\pmb{x}_i))^2\)，两者均为 \(\|\cdot\|_2^2\) 范数在边界离散点上的均值近似。

---

#### 公式 (8)：含惩罚项的修正势能泛函

**底层计算代码**  
[`Rubber_Ironing/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/OPT.py)

```python
# OPT.py — Rubber_Ironing（总损失：Π + κ·Π_EBC）
            loss = l1 + l2 + l3 + l4*1e4 + l5*1e4
            #      ^^^^^^^^^^^    ^^^^^^^^^^^^^^
            #        Π（应变能+接触能）  κ·Π_EBC（惩罚因子 κ=1e4）
```

**公式 (8)**

\[
\Pi^{*} = \Pi + \kappa \, \Pi_{\mathrm{EBC}}
\]

**解读**：`loss` 即修正势能泛函 \(\Pi^*\)，惩罚因子 \(\kappa = 10^4\)（代码中 `1e4`）使软边界条件项在训练中得到足够权重，使预测位移趋近规定边界值 \(\bar{\pmb{u}}\)。

---

#### 公式 (9)：硬边界条件施加（神经网络输出修正）

**底层计算代码**  
[`Rubber_Ironing/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py)

```python
# FNN.py — Rubber_Ironing（硬边界条件：距离函数与偏置叠加）
class time_layer(tf.keras.layers.Layer):
    """距离函数 g(x) = (x_t - variable)，确保边界处输出为零"""
    def call(self, inputs, x):
        return inputs * (x - self.variable)   # F(x) ⊙ g(x)

class plus_untrainable_layer(tf.keras.layers.Layer):
    """在 F(x)⊙g(x) 上叠加规定位移 ū"""
    def call(self, inputs):
        return inputs + self.variable          # + ū(x)

class plus_trainable_layer(tf.keras.layers.Layer):
    """可训练偏置：用于逐步加载时规定位移为可学习参数"""
    def call(self, inputs):
        return inputs + self.bias              # + ū(x)（可训练）
```

[`Rubber_Ironing/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py)

```python
# FNN.py — net() 中将 time_layer 和 plus_layer 串联
    def net(self):
        x = tf.keras.layers.Input(shape=(self.n_in,))
        temp = x
        for l in self.layers:
            temp = tf.keras.layers.Dense(l, activation=self.acti_fun, kernel_initializer=self.k_init)(temp)
        y = tf.keras.layers.Dense(self.n_out, kernel_initializer=self.k_init, use_bias=False)(temp)

        if self.time_layer:
            t_l = time_layer(self.t_variable)
            y = t_l(y, x[..., self.t_in-1, tf.newaxis],)  # y = F(x) ⊙ g(x)

        if self.trainable_pl:
            p_l = plus_trainable_layer()
            y = p_l(y)                                       # y = F(x)⊙g(x) + ū（可训练）
        else:
            p_l = plus_untrainable_layer(self.p_variable)
            y = p_l(y)                                       # y = F(x)⊙g(x) + ū（固定）
        nn = tf.keras.models.Model(inputs=x, outputs=y)
        return nn
```

**公式 (9)**

\[
\pmb{u}(\pmb{x}) = \pmb{F}(\pmb{x}) \odot \pmb{g}(\pmb{x}) + \bar{\pmb{u}}(\pmb{x})
\]

**解读**：`time_layer` 实现距离函数 \(\pmb{g}(\pmb{x}) = x_{t\_in} - t\_variable\) 与网络输出 \(\pmb{F}(\pmb{x})\) 的逐元素积 \(\pmb{F}(\pmb{x})\odot\pmb{g}(\pmb{x})\)；`plus_untrainable_layer`（或 `plus_trainable_layer`）叠加规定位移 \(\bar{\pmb{u}}\)，合计即为满足本质边界条件的位移场。

---

#### 公式 (10)：距离函数定义

**底层计算代码**  
[`Rubber_Ironing/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py)

```python
# FNN.py — time_layer：实现从点 x 到边界的距离函数（1D 简化形式）
class time_layer(tf.keras.layers.Layer):
    def __init__(self, variable):
        super().__init__()
        self.unit = 1
        self.variable = variable   # 边界坐标值 y_0

    def call(self, inputs, x):
        # g(x) = x - y_0，当 x = y_0 时 g = 0（点在边界上）
        return inputs * (x - self.variable)
```

**公式 (10)**

\[
\pmb{g}(\pmb{x}) = \min_{\pmb{y} \in \Gamma^{\pmb{u}}} \|\pmb{x} - \pmb{y}\|_2^2
= \min_{\pmb{y} \in \Gamma^{\pmb{u}}} \left[ (\pmb{x} - \pmb{y}) \cdot (\pmb{x} - \pmb{y}) \right]
\]

**解读**：在代码中，对于平直边界 \(\Gamma^{\mathrm{u}}: x_{t\_in} = t\_variable\)，距离函数简化为 \(g(\pmb{x}) = x_{t\_in} - t\_variable\)（即一维有符号距离），等价于公式 (10) 在 \(\Gamma^{\mathrm{u}}\) 为坐标轴对齐平面时的退化形式。

---

### 2.3 Neural network training and its pseudo-dynamic nature

---

#### 公式 (11)：伪速度

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 自动微分计算梯度 ∂Π/∂θ（即伪速度的核心）
    @tf.function
    def Loss(self, x, y):
        with tf.GradientTape() as g:
            tmp = self.pirbn(x)
            # ... 损失计算 ...
            loss = l1 + l2 + l3 + l4 + l5 + l6 + l7 + l8 + l9
        # ∂Π/∂θ（梯度 = ∂F^T/∂θ · ∂Π/∂u · ... 反向传播结果）
        grads = g.gradient(loss, self.pirbn.trainable_variables)
        return loss, grads, l1, l2, l3, l4, l5, l6, l7, l8, l9
```

**公式 (11)**

\[
\frac{\partial \pmb{u}}{\partial t} \approx -\frac{\partial \pmb{F}(\pmb{x};\pmb{\theta})^{\mathrm{T}}}{\partial \pmb{\theta}} \frac{\partial \Pi}{\partial \pmb{\theta}}
\]

**解读**：`g.gradient(loss, self.pirbn.trainable_variables)` 通过 TensorFlow 自动微分计算 \(\partial \Pi / \partial \pmb{\theta}\)，配合反向传播中的 Jacobian \(\partial \pmb{F}^{\mathrm{T}}/\partial \pmb{\theta}\)，即为位移场的伪速度 \(\partial \pmb{u}/\partial t\)。这一梯度信息被 Adam 优化器用于更新参数（公式 A.2–A.7）。

---

#### 公式 (12)：位移增量

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — Adam 参数更新：Δθ = -η · m̂/√(v̂+ε)（近似 Δu ≈ η·∂u/∂t）
        for i in range(0,self.maxiter):
            loss, g = self.evaluate(x)
            m = (1 - beta1) * g + beta1 * m
            v = (1 - beta2) * (g**2) + beta2 * v
            mhat = m / (1 - beta1**(i + 1))
            vhat = v / (1 - beta2**(i + 1))
            # Δθ = -η · m̂/（√v̂ + ε）≈ -η · ∂Π/∂θ
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)
```

**公式 (12)**

\[
\Delta \pmb{u} \approx \eta \frac{\partial \pmb{u}}{\partial t}
\]

**解读**：代码中每次迭代的参数更新量 \(\Delta\pmb{\theta} = -\eta \cdot \hat{m}/(\sqrt{\hat{v}}+\varepsilon)\) 通过链式法则传递到位移增量 \(\Delta\pmb{u} \approx (\partial\pmb{F}^{\mathrm{T}}/\partial\pmb{\theta})\Delta\pmb{\theta}\)，学习率 \(\eta\)（`learning_rate`）直接控制每步位移增量的幅度。

---

## 3. Frictionless contact potentials and numerical implementations

### 3.1 Frictionless surface contact potential

---

#### 公式 (13)：含接触能的总势能

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 总损失：应变能 + 接触势能（E_in + E_c，无外力项 E_ex）
    @tf.function
    def Loss(self, x, y):
        with tf.GradientTape() as g:
            tmp = self.pirbn(x)
            l1 = tf.reduce_sum(tmp[0] * y[0])    # E_in,1（物体 1 应变能）
            l2 = tf.reduce_sum(tmp[1] * y[1])    # E_in,2（物体 2 应变能）
            l3 = tf.reduce_sum(tmp[2]) * y[2]    # E_c,a（顶部接触：cap-ring1）
            l4 = tf.reduce_sum(tmp[3]) * y[3]    # E_c,b（顶部接触：cap-ring2）
            l5 = tf.reduce_sum(tmp[4]) * y[2]    # E_c,c（底部接触：ring1-sink）
            l6 = tf.reduce_sum(tmp[5]) * y[3]    # E_c,d（底部接触：ring2-sink）
            l7 = tf.reduce_sum(tmp[6]) * y[2]    # E_c,e（侧面接触：ring1-sink）
            l8 = tf.reduce_sum(tmp[7]) * y[3]    # E_c,f（侧面接触：ring2-sink）
            l9 = tf.reduce_sum(tmp[8]) * 1e-3    # E_c,r（环-环接触）
            loss = l1 + l2 + l3 + l4 + l5 + l6 + l7 + l8 + l9
        grads = g.gradient(loss, self.pirbn.trainable_variables)
        return loss, grads, l1, l2, l3, l4, l5, l6, l7, l8, l9
```

**公式 (13)**

\[
\Pi = E_{\mathrm{in}} - E_{\mathrm{ex}} + E_{\mathrm{c}}
\]

**解读**：`loss = l1 + l2 + l3 + ... + l9` 即扩展后的总势能 \(\Pi\)，其中 `l1 + l2` 为总应变能 \(E_{\mathrm{in}}\)，`l3`–`l9` 为各接触面的接触势能 \(E_{\mathrm{c}}\)（包括环-帽接触、环-槽底接触、环-槽侧接触及环-环接触）。本问题中无集中外力，故 \(E_{\mathrm{ex}} = 0\)。

---

#### 公式 (14)：接触势能面积分

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py)

```python
# PINN.py — 接触势能计算（对两接触面离散点的外积求和近似二重面积分）
    ### Contact energy
    a = 1e3
    b = 1e-2
    # exp 型接触势能
    e_a = b * tf.exp(-a * dV_a)    # ∬ β₁β₂φ(r) dΓ₁dΓ₂（cap 顶部 vs ring1）
    e_b = b * tf.exp(-a * dV_b)    # （cap 顶部 vs ring2）
    e_c = b * tf.exp(-a * dV_c)    # （sink 底部 vs ring1）
    e_d = b * tf.exp(-a * dV_d)    # （sink 底部 vs ring2）
    e_e = b * tf.exp(-a * dU_e)    # （sink 左侧 vs ring1）
    e_f = b * tf.exp(-a * dU_f)    # （sink 右侧 vs ring2）
    e_r = b * tf.exp(-a * r)       # （ring1 vs ring2，点对点矩阵）
```

[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 求和近似面积分（乘以面积微元 dr）
    l3 = tf.reduce_sum(tmp[2]) * y[2]    # ≈ ∫_{Γ₁}∫_{Γ₂} β₁β₂φ(r) dΓ₁dΓ₂（乘以 dr1）
    l4 = tf.reduce_sum(tmp[3]) * y[3]
    l9 = tf.reduce_sum(tmp[8]) * 1e-3   # 环-环接触（PP 模型，额外缩放因子）
```

**公式 (14)**

\[
E_{\mathrm{c}} = \int_{\Gamma_1} \int_{\Gamma_2} \beta_1 \beta_2 \phi(r) \, \mathrm{d}\Gamma_1 \, \mathrm{d}\Gamma_2
\]

**解读**：代码中 `tf.exp(-a * dV_a)` 等项计算每个接触点对的势能 \(\phi(r)\)，`tf.reduce_sum` 对所有接触点求和、再乘以面积微元 `dr`（即 `y[2]`、`y[3]`），近似实现了两接触面上的二重积分 \(\int_{\Gamma_1}\int_{\Gamma_2}\beta_1\beta_2\phi(r)\,\mathrm{d}\Gamma_1\mathrm{d}\Gamma_2\)。

---

#### 公式 (15)：指数型接触势能

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py)

```python
# PINN.py — 指数型接触势能（点对面 PS 模型：各面单独计算）
    a = 1e3    # 1/r₀（衰减系数）
    b = 1e-2   # φ₀（势能常数）

    # 各接触面与刚体边界的法向距离
    dV_a = (1.15 - 0.05 * j) - V1_cont    # ring1 顶部与 cap 的间距
    dV_b = (1.15 - 0.05 * j) - V2_cont    # ring2 顶部与 cap 的间距
    dV_c = V1_cont                         # ring1 底部与 sink 底面的间距
    dV_d = V2_cont                         # ring2 底部与 sink 底面的间距
    dU_e = U1_cont                         # ring1 左侧与 sink 侧面的间距
    dU_f = 1.25 - U2_cont                  # ring2 右侧与 sink 侧面的间距

    # φ(r) = φ₀·exp(-r/r₀) = b·exp(-a·r)
    e_a = b * tf.exp(-a * dV_a)
    e_b = b * tf.exp(-a * dV_b)
    e_c = b * tf.exp(-a * dV_c)
    e_d = b * tf.exp(-a * dV_d)
    e_e = b * tf.exp(-a * dU_e)
    e_f = b * tf.exp(-a * dU_f)
```

[`Rubber_Ironing/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/PINN.py)

```python
# PINN.py — Rubber_Ironing（指数型接触势能，PP 模型）
    ### repulsive force
    a = 1e2
    b = 1e2
    Erepulsive = b * tf.exp(-a * r)    # φ(r) = φ₀·exp(-r/r₀)
```

**公式 (15)**

\[
\phi(r) = \phi_0 \exp\!\left(-\frac{r}{r_0}\right)
\]

**解读**：代码中 `b * tf.exp(-a * distance)` 直接实现了指数型接触势能，其中 `b` 对应 \(\phi_0\)，`a` 对应 \(1/r_0\)（衰减系数的倒数）。当接触面间距 \(r\to 0\) 时，势能迅速增大，防止穿透；当 \(r\to\infty\) 时势能趋向零。

---

### 3.2 Numerical implementation

> **说明**：公式 (16) 和 (17) 在原始 Markdown 文件中未出现，但从代码推断它们对应 Neo-Hookean 超弹性材料的变形梯度 \(\pmb{F}\)（Eq. 16）及其应变能密度 \(\Psi(\pmb{F})\)（Eq. 17）。相关代码见下方 PINN.py 中变形梯度与应变能密度的计算部分（同时对应公式 4 的底层实现细节）。

---

#### 公式 (16/17)：变形梯度与 Neo-Hookean 应变能密度（从代码推断）

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py)

```python
# PINN.py — 变形梯度分量、Jacobian、第一不变量及 Neo-Hookean 应变能密度
    ### plain stress（Lamé 参数：λ = νE/((1+ν)(1-2ν))，μ = E/(2(1+ν))）
    la = mu / (1 + mu) / (1 - 2 * mu) * E    # λ（第一 Lamé 常数）
    nu = 1 / (1 + mu) / 2 * E                # μ（剪切模量）

    # 变形梯度 F（2D 分量）
    F11_1 = (1 + U1_x)    # F₁₁ = 1 + ∂u/∂x
    F22_1 = (1 + V1_y)    # F₂₂ = 1 + ∂v/∂y
    F12_1 = U1_y           # F₁₂ = ∂u/∂y
    F21_1 = V1_x           # F₂₁ = ∂v/∂x

    # Jacobian 行列式 J = det(F) = F₁₁F₂₂ - F₁₂F₂₁
    J1 = F11_1 * F22_1 - F12_1 * F21_1

    # 第一不变量 I₁ = tr(FᵀF) = F₁₁² + F₁₂² + F₂₁² + F₂₂²
    I1 = (F11_1 ** 2 + F12_1 ** 2 + F21_1 ** 2 + F22_1 ** 2)

    # Neo-Hookean 应变能密度：Ψ = (λ/4)(J²-1) - (λ/2+μ)ln(J) + (μ/2)(I₁-2)
    e1 = 0.25 * la* (J1 ** 2 - 1) - (la / 2 + nu) * tf.math.log(J1) + 0.5 * nu * (I1 - 2)
```

[`Compression_of_Two_Rubber_Rings/Dif_op.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/Dif_op.py)

```python
# Dif_op.py — 自动微分计算位移偏导数（变形梯度分量来源）
class Dif(tf.keras.layers.Layer):

    def __init__(self, fnn, **kwargs):
        self.fnn = fnn
        super().__init__(**kwargs)
    
    def call(self, xy):
        x, y = (xy[..., i, tf.newaxis] for i in range(xy.shape[-1]))
        with tf.GradientTape(persistent=True) as g:
            g.watch(x)
            g.watch(y)
            U = self.fnn(tf.concat([x, y], axis=-1))

        U_x = g.gradient(U, x)    # ∂u/∂x（变形梯度分量）
        U_y = g.gradient(U, y)    # ∂u/∂y（变形梯度分量）
        del g

        return U_x, U_y
```

**变形梯度（推断公式 16）**

\[
\pmb{F} = \begin{bmatrix} 1 + \dfrac{\partial u}{\partial x} & \dfrac{\partial u}{\partial y} \\[6pt] \dfrac{\partial v}{\partial x} & 1 + \dfrac{\partial v}{\partial y} \end{bmatrix}, \quad
J = \det(\pmb{F}) = F_{11}F_{22} - F_{12}F_{21}, \quad
I_1 = \mathrm{tr}(\pmb{F}^{\mathrm{T}}\pmb{F})
\]

**Neo-Hookean 应变能密度（推断公式 17）**

\[
\Psi(\pmb{F}) = \frac{\lambda}{4}(J^2 - 1) - \left(\frac{\lambda}{2} + \mu\right)\ln J + \frac{\mu}{2}(I_1 - 2)
\]

其中 \(\lambda = \dfrac{\nu E}{(1+\nu)(1-2\nu)}\)，\(\mu = \dfrac{E}{2(1+\nu)}\)。

**解读**：`Dif` 类（`Dif_op.py`）利用 `tf.GradientTape` 自动微分计算偏导数 \(\partial u/\partial x\) 等变形梯度分量；`PINN.py` 据此构造变形梯度 \(\pmb{F}\)、行列式 \(J\) 和第一不变量 \(I_1\)，最终计算 Neo-Hookean 应变能密度 \(\Psi(\pmb{F})\)。

---

#### 公式 (18)：接触点相对距离矩阵

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py)

```python
# PINN.py — 环-环接触点相对距离矩阵（n×m）
    # 变形后接触点坐标
    U1_cont = fu1(x1_cont) + x1_cont[...,0,tf.newaxis]+0.35   # ring1 水平坐标
    V1_cont = fv1(x1_cont) + x1_cont[..., 1, tf.newaxis]+0.35  # ring1 竖向坐标

    U2_cont = fu2(x2_cont) + x2_cont[..., 0, tf.newaxis]+0.9  # ring2 水平坐标
    V2_cont = fv2(x2_cont) + x2_cont[..., 1, tf.newaxis]+0.8  # ring2 竖向坐标

    # 外积：ΔU_{n×m} = U1_{n×1}⊗1_{m×1} - (U2_{m×1}⊗1_{n×1})ᵀ
    dU_r = U1_cont * cost1 - tf.transpose(U2_cont * cost2)
    dV_r = V1_cont * cost1 - tf.transpose(V2_cont * cost2)

    # r_{n×m} = √((ΔU)² + (ΔV)²)
    r = tf.sqrt(tf.square(dU_r) + tf.square(dV_r))
```

**公式 (18)**

\[
r_{n \times m} = \sqrt{(\Delta \pmb{u})^2 + (\Delta \pmb{v})^2}
\]

**解读**：`r = tf.sqrt(tf.square(dU_r) + tf.square(dV_r))` 直接计算两接触面 \(n\) 个与 \(m\) 个点之间的全部距离，结果为 \(n\times m\) 矩阵。`cost1`、`cost2` 是积分权重（弧长微元），使外积运算同时嵌入了面积分离散。

---

#### 公式 (19)：外积构造差值矩阵

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py)

```python
# PINN.py — 外积（outer product）构造 ΔU_{n×m} 和 ΔV_{n×m}
    # U1_cont: shape (n, 1)，U2_cont: shape (m, 1)
    # cost1: shape (n, 1)（ring1 弧长权重），cost2: shape (m, 1)（ring2 弧长权重）
    # 利用 TensorFlow broadcasting 实现外积：
    # dU_r[i,j] = U1_cont[i] * cost1[i] - U2_cont[j] * cost2[j]
    dU_r = U1_cont * cost1 - tf.transpose(U2_cont * cost2)
    dV_r = V1_cont * cost1 - tf.transpose(V2_cont * cost2)
```

[`Rubber_Ironing/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/PINN.py)

```python
# PINN.py — Rubber_Ironing（外积构造接触距离矩阵）
    dU = U1_cont * cost1 - tf.transpose(U2_cont * cost2)
    dV = V1_cont * cost1 - tf.transpose(V2_cont * cost2)
    r = tf.sqrt(tf.square(dU) + tf.square(dV))
```

**公式 (19)**

\[
\begin{array}{r}
\Delta \pmb{u}_{n \times m} = \pmb{x}_{n \times 1} \otimes \pmb{1}_{m \times 1} - (\pmb{x}_{m \times 1} \otimes \pmb{1}_{n \times 1})^{\mathrm{T}} \\
\Delta \pmb{v}_{n \times m} = \pmb{y}_{n \times 1} \otimes \pmb{1}_{m \times 1} - (\pmb{y}_{m \times 1} \otimes \pmb{1}_{n \times 1})^{\mathrm{T}}
\end{array}
\]

**解读**：TensorFlow 2 中 `(n,1)` 张量与 `(1,m)` 张量相乘即为外积 \(\otimes\)。代码中 `U1_cont * cost1`（形状 `(n,1)`）减去 `tf.transpose(U2_cont * cost2)`（转置后形状 `(1,m)` 广播到 `(n,m)`），直接实现公式 (19) 中的外积差值矩阵 \(\Delta\pmb{u}_{n\times m}\)。

---

#### 公式 (20)：接触势能矩阵

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py)

```python
# PINN.py — 对距离矩阵 r_{n×m} 逐元素计算接触势能
    a = 1e3    # 1/r₀
    b = 1e-2   # φ₀
    # φ(r)_{n×m} = φ₀·exp(-r_{n×m}/r₀)
    e_r = b * tf.exp(-a * r)    # shape: (n, m)
```

[`Rubber_Ironing/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/PINN.py)

```python
# PINN.py — Rubber_Ironing（PP 接触势能矩阵）
    a = 1e2
    b = 1e2
    Erepulsive = b * tf.exp(-a * r)    # φ(r)_{n×m} = φ₀·exp(-r/r₀)
```

**公式 (20)**

\[
\phi(r)_{n \times m} = \phi_0 \exp\!\left(-\frac{r_{n \times m}}{r_0}\right)
\]

**解读**：代码中 `b * tf.exp(-a * r)` 对距离矩阵 `r`（形状 \(n\times m\)）逐元素计算指数型势能，得到接触势能矩阵 \(\phi(r)_{n\times m}\)。在 `OPT.py` 中，`tf.reduce_sum` 对该矩阵求和并乘以面积微元，完成公式 (14) 的二重积分近似。

---

### 3.3 Relaxation, gradual loading and output scaling

---

#### 公式 (21)：渐进加载软边界条件

**底层计算代码**  
[`Rubber_Ironing/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/OPT.py)

```python
# OPT.py — Rubber_Ironing（渐进加载：iter 作为当前加载量 t·ū/t_max）
    def evaluate(self, weights):
        self.set_weights(weights)
        if self.t_or_r==0:
            # 加载阶段：位移目标随 iter 线性增长（t/t_max · ū）
            ttt = tf.constant((self.i - 1) * 0.1 + self.iter * 0.1 * 0.5e-3)
        else:
            # 松弛阶段：固定目标位移
            ttt = tf.constant((self.i - 0) * 0.1)
        loss, grads, l1, l2, l3, l4, l5 = self.Loss(self.x_train, self.y_train, ttt)
        ...

    @tf.function
    def Loss(self, x, y, iter):
        with tf.GradientTape() as g:
            tmp = self.pinn(x)
            l1 = tf.reduce_sum(tmp[0]*y[0])
            l2 = tf.reduce_sum(tmp[1]*y[1])
            l3 = tf.reduce_sum(tmp[2]*y[2]*y[2])
            # Π_EBC(t) = mean((V(x) - t/t_max · V̄)²)，iter 对应 t/t_max·V̄
            l4 = tf.reduce_mean(tf.square(tmp[3]+iter))
            # Π_EBC for U = mean((U(x) - 0)²)
            l5 = tf.reduce_mean(tf.square(tmp[4]))
            loss = l1+l2+l3+l4*1e4+l5*1e4
        grads = g.gradient(loss, self.pinn.trainable_variables)
        return loss, grads, l1, l2, l3, l4, l5
```

**公式 (21)**

\[
\Pi_{\mathrm{EBC}}(t) = \frac{1}{n} \left\| \pmb{u}(\pmb{x}) - \frac{t}{t_{\max}} \bar{\pmb{u}}(\pmb{x}) \right\|_2^2
\]

**解读**：代码中 `ttt = (i-1)*0.1 + iter*0.1*0.5e-3` 随训练迭代次数 `iter` 线性增大，模拟 \(t/t_{\max}\cdot\bar{V}\) 的渐进加载过程；`l4 = tf.reduce_mean(tf.square(tmp[3] + iter))` 对应 \(\Pi_{\mathrm{EBC}}(t)\)，随 `iter` 增大，边界位移目标逐渐趋近规定值 \(\bar{\pmb{u}}\)。

---

#### 公式 (22)：输出缩放

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/FNN.py)

```python
# FNN.py — Compression_of_Two_Rubber_Rings（缩放因子 f = ξ）
def FNN(n_input, n_output, layers, f = 1., acti_fun='tanh', k_init='LecunNormal'):
    x = tf.keras.layers.Input(shape=(n_input))
    temp = x
    for l in layers:
        temp = tf.keras.layers.Dense(l, activation = acti_fun, kernel_initializer=k_init)(temp)
    # 输出层：无偏置，乘以缩放因子 f = ξ
    y = tf.keras.layers.Dense(n_output, kernel_initializer=k_init, use_bias = False)(temp) * f
    net = tf.keras.models.Model(inputs=x, outputs=y)
    return net
```

[`Compression_of_Two_Rubber_Rings/Main.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/Main.py)

```python
# Main.py — 创建 FNN 时指定缩放因子（Hertz 接触基准测试中 ξ = 1e-3）
fu1 = FNN(n_in, n_out, [50, 50, 50], f = 1., acti_fun='tanh', k_init='LecunNormal')
fv1 = FNN(n_in, n_out, [50, 50, 50], f = 1., acti_fun='tanh', k_init='LecunNormal')
```

**公式 (22)**

\[
\pmb{u}(\pmb{x}) = \xi \bigl( \pmb{F}(\pmb{x}) \odot \pmb{g}(\pmb{x}) + \bar{\pmb{u}}(\pmb{x}) \bigr)
\]

**解读**：`FNN` 函数中输出层后乘以 `f`（即缩放因子 \(\xi\)），使神经网络初始输出量级与实际位移场相匹配。对于小变形 Hertz 接触问题，\(\xi = 10^{-3}\) 可显著加速收敛；对于大变形问题（如橡皮环压缩），令 \(\xi = 1\)。

---

### 3.4 Benchmark tests and discussions

---

#### 公式 (23)：Hertz 接触基准问题神经网络输出

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/FNN.py)

```python
# FNN.py — 缩放因子与输入变量相乘实现硬边界（U = ξ·x·Fx(x,y)）
def FNN(n_input, n_output, layers, f = 1., acti_fun='tanh', k_init='LecunNormal'):
    x = tf.keras.layers.Input(shape=(n_input))
    temp = x
    for l in layers:
        temp = tf.keras.layers.Dense(l, activation = acti_fun, kernel_initializer=k_init)(temp)
    y = tf.keras.layers.Dense(n_output, kernel_initializer=k_init, use_bias = False)(temp) * f
    net = tf.keras.models.Model(inputs=x, outputs=y)
    return net
```

[`Rubber_Ironing/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py)

```python
# FNN.py — Rubber_Ironing（time_layer 实现 U = F(x)·(x - x_bc)，等价于 U = ξ·x·Fx）
class time_layer(tf.keras.layers.Layer):
    def __init__(self, variable):
        super().__init__()
        self.variable = variable

    def call(self, inputs, x):
        # 对称边界条件：x=0 时 U=0 → inputs * (x - 0) = x · F_x(x,y)
        return inputs * (x - self.variable)
```

**公式 (23)**

\[
\begin{array}{l}
U = \xi \, x \, F_x(x, y), \\
V = \xi \, F_y(x, y).
\end{array}
\]

**解读**：对于 Hertz 基准问题，\(U\) 的输出通过 `time_layer`（令 `t_variable=0`, `t_in=1`）实现 \(U = F_x(x,y)\cdot x\)（在 \(x=0\) 处对称轴上 \(U=0\)），再乘以缩放因子 \(\xi\)；\(V\) 直接输出 \(V = \xi\cdot F_y(x,y)\)，不施加额外硬边界条件。

---

## 4. Numerical examples

### 4.1 Rubber ironing

---

#### 公式 (24)：橡皮熨烫问题神经网络输出结构

**底层计算代码**  
[`Rubber_Ironing/FNN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py)

```python
# FNN.py — Rubber_Ironing（完整 FNN 类：time_layer + plus_layer 实现公式 24）
class time_layer(tf.keras.layers.Layer):
    def __init__(self, variable):
        super().__init__()
        self.unit = 1
        self.variable = variable

    def call(self, inputs, x):
        # U_c = NN(x,y) · (y - t_variable) + p_variable
        # 当 y = t_variable（半圆柱顶面）时 NN·(y-t_variable) = 0，满足 u|_{top} = 0 或 v|_{top} = V_t
        return inputs * (x - self.variable)

class plus_untrainable_layer(tf.keras.layers.Layer):
    def __init__(self, variable):
        super().__init__()
        self.variable = variable

    def call(self, inputs):
        return inputs + self.variable

class plus_trainable_layer(tf.keras.layers.Layer):
    def __init__(self):
        super().__init__()
        self.unit = 1

    def build(self, input_shape):
        self.bias=self.add_weight(
            shape = (self.unit,),
            initializer="zero",
            trainable = True,
            name="bias",
        )

    def call(self, inputs):
        return inputs + self.bias
```

[`Rubber_Ironing/Main.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/Main.py)

```python
# Main.py — 创建四个 FNN，分别对应 U_c, V_c, U_s, V_s（公式 24 四个输出）
fu1 = FNN(n_in, n_out, layer, time_layer = True, t_in = 2, t_variable = 1,
          trainable_plus_layer = False, p_variable = 0).build()
# → U_c = NN_u1(x,y) · (y - 1)  （硬 BC：y=1 处 U=0）

fv1 = FNN(n_in, n_out, layer, time_layer = True, t_in = 2, t_variable = 1,
          trainable_plus_layer = True, p_variable = 0.1).build()
# → V_c = NN_v1(x,y) · (y - 1) + V_t  （可训练偏置 V_t 对应当前加载位移）

fu2 = FNN(n_in, n_out, layer, time_layer = True, t_in = 2, t_variable = -1.5).build()
# → U_s = NN_u2(x,y) · (y - (-1.5))  （硬 BC：y=-1.5 底面 U=0）

fv2 = FNN(n_in, n_out, layer, time_layer = True, t_in = 2, t_variable = -1.5).build()
# → V_s = NN_v2(x,y) · (y - (-1.5))  （硬 BC：y=-1.5 底面 V=0）
```

[`Rubber_Ironing/PINN.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/PINN.py)

```python
# PINN.py — Rubber_Ironing（变形后坐标：位移输出叠加参考坐标）
    U1_cont = fu1(x1_cont) + x1_cont[...,0,tf.newaxis]      # X = U_c + x₀
    V1_cont = fv1(x1_cont) + x1_cont[..., 1, tf.newaxis] + 2.0  # Y = V_c + y₀ + 2.0
    V1_t = fv1(x1_t)    # 顶面处 V_c（软 BC 惩罚用）
    U1_t = fu1(x1_t)    # 顶面处 U_c（软 BC 惩罚用）

    U2_cont = fu2(x2_cont) + x2_cont[..., 0, tf.newaxis]    # X = U_s + x₀
    V2_cont = fv2(x2_cont) + x2_cont[..., 1, tf.newaxis] + 1.5  # Y = V_s + y₀ + 1.5
```

**公式 (24)**

\[
\begin{array}{l}
U_{\mathrm{c}} = (y - 3)\, u_{\mathrm{c}}(x,y) + U_{\mathrm{t}}, \\
V_{\mathrm{c}} = (y - 3)\, v_{\mathrm{c}}(x,y) + V_{\mathrm{t}}, \\
U_{\mathrm{s}} = y \, u_{\mathrm{s}}(x,y), \\
V_{\mathrm{s}} = y \, v_{\mathrm{s}}(x,y).
\end{array}
\]

**解读**：
- `fu1` 和 `fv1` 对应半圆柱的水平和竖向位移 \(U_{\mathrm{c}}\)、\(V_{\mathrm{c}}\)，`time_layer`（`t_variable=1`）乘以 \((y-1)\) 在顶面 \(y=1\) 精确施加 \(U=0\)（`fu1`）和 \(V=V_t\)（`fv1` 的 `plus_trainable_layer`，`V_t` 为可训练位移加载量）；
- `fu2` 和 `fv2` 对应平板的水平和竖向位移 \(U_{\mathrm{s}}\)、\(V_{\mathrm{s}}\)，`time_layer`（`t_variable=-1.5`）乘以 \((y-(-1.5)) = y+1.5\) 在底面 \(y=-1.5\) 施加零位移 \(U=V=0\)；
- 论文中 \(y-3\) 与代码中 \(y-1\) 的差异来自坐标系原点的选取不同，但结构上完全一致。

---

## Appendix A. The pseudo-velocity of the energy-based PINNs during the training process

---

#### 公式 (A.1)：当前 epoch 的位移预测

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 第 t 步的位移预测（神经网络前向推理）
    @tf.function
    def Loss(self, x, y):
        with tf.GradientTape() as g:
            tmp = self.pirbn(x)    # uᵗ = F(x; θᵗ)（当前参数 θᵗ 的网络输出）
            ...
```

**公式 (A.1)**

\[
\pmb{u}^t = \pmb{F}(\pmb{x};\, \pmb{\theta}^t)
\]

**解读**：`tmp = self.pirbn(x)` 即以当前参数 \(\pmb{\theta}^t\) 对坐标 \(\pmb{x}\) 进行前向推理，得到第 \(t\) 步的位移预测 \(\pmb{u}^t\)。

---

#### 公式 (A.2)：梯度下降参数更新

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 普通梯度下降（VGD）参数更新（Adam 的特例：β₁=β₂=0, ε=0）
# 完整 Adam 更新：
        for i in range(0, self.maxiter):
            loss, g = self.evaluate(x)
            m = (1 - beta1) * g + beta1 * m               # 一阶矩
            v = (1 - beta2) * (g**2) + beta2 * v          # 二阶矩
            mhat = m / (1 - beta1**(i + 1))               # 偏差修正
            vhat = v / (1 - beta2**(i + 1))
            # θᵗ⁺¹ = θᵗ - η · m̂/(√v̂ + ε)（≈ θᵗ - η·∂Π/∂θ）
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)
```

**公式 (A.2)**

\[
\pmb{\theta}^{t+1} = \pmb{\theta}^t - \eta \left. \frac{\partial \Pi}{\partial \pmb{\theta}} \right|_{\pmb{\theta}^t}
\]

**解读**：Adam 算法是公式 (A.2) VGD 的自适应版本。代码中 `x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)` 中的 `mhat / (np.sqrt(vhat) + eps)` 是对 \(\partial\Pi/\partial\pmb{\theta}\) 的预处理版本（通过一阶矩 \(\hat{m}\) 和二阶矩 \(\hat{v}\) 自适应调整学习率），`learning_rate` 即 \(\eta\)。

---

#### 公式 (A.3)：参数增量

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 参数增量 Δθ = θᵗ⁺¹ - θᵗ = -η·m̂/(√v̂+ε)
            # 当前参数
            x_old = x.copy()  # θᵗ（隐含）
            # 更新后参数
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)  # θᵗ⁺¹
            # Δθ = x - x_old = -learning_rate * mhat / (np.sqrt(vhat) + eps)
```

**公式 (A.3)**

\[
\Delta \pmb{\theta}^{t+1} = \pmb{\theta}^{t+1} - \pmb{\theta}^t = -\eta \left. \frac{\partial \Pi}{\partial \pmb{\theta}} \right|_{\pmb{\theta}^t}
\]

**解读**：每次迭代中，`-learning_rate * mhat / (np.sqrt(vhat) + eps)` 即为参数增量 \(\Delta\pmb{\theta}^{t+1}\)（Adam 中带动量和自适应学习率修正），其本质是沿损失函数负梯度方向的移动，与 VGD 的 \(-\eta\,\partial\Pi/\partial\pmb{\theta}\) 等价。

---

#### 公式 (A.4)：Taylor 展开（位移场增量近似）

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 隐式 Taylor 展开：新参数下的网络输出通过 TF 自动推理（即 F(x;θᵗ⁺¹)）
    def evaluate(self, weights):
        # 更新权重到 θᵗ⁺¹
        self.set_weights(weights)
        # 计算 F(x;θᵗ⁺¹) = F(x;θᵗ) + ∂F/∂θ·Δθ + O(Δθ²)（一阶近似）
        loss, grads, l1, l2, l3, l4, l5, l6, l7, l8, l9 = self.Loss(self.x_train, self.y_train)
        ...
```

**公式 (A.4)**

\[
\pmb{u}^{t+1}(\pmb{x};\pmb{\theta}^{t+1})
= \pmb{F}(\pmb{x};\pmb{\theta}^t) + \frac{\partial \pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial \pmb{\theta}}(\Delta\pmb{\theta}^{t+1}) + O(\Delta\pmb{\theta}^{t+1})
\approx \pmb{F}(\pmb{x};\pmb{\theta}^t) + \frac{\partial \pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial \pmb{\theta}}(\Delta\pmb{\theta}^{t+1})
\]

**解读**：代码中每次 `evaluate` 先通过 `set_weights(weights)` 将参数从 \(\pmb{\theta}^t\) 更新到 \(\pmb{\theta}^{t+1}\)，再计算新位移 \(\pmb{u}^{t+1} = \pmb{F}(\pmb{x};\pmb{\theta}^{t+1})\)，这在数学上等价于公式 (A.4) 的 Taylor 一阶截断近似（忽略高阶项 \(O(\Delta\pmb{\theta}^{t+1})\)）。

---

#### 公式 (A.5)：位移增量

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 位移增量隐式计算：Δu = F(x;θᵗ⁺¹) - F(x;θᵗ)
# 对应 Adam 中的实际更新量（由两次连续 evaluate 的输出差得到）:
        for i in range(0, self.maxiter):
            loss, g = self.evaluate(x)         # 计算损失和梯度（内含 F(x;θᵗ)）
            # ... Adam 更新 ...
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)  # θᵗ→θᵗ⁺¹
            # 下一次 evaluate 将计算 F(x;θᵗ⁺¹)，其与当前输出之差即为 Δu
```

**公式 (A.5)**

\[
\Delta \pmb{u}^{t+1} = \pmb{u}^{t+1} - \pmb{u}^t
\approx \frac{\partial \pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial \pmb{\theta}} (\Delta\pmb{\theta}^{t+1})
\]

**解读**：两次连续前向推理输出之差即为位移增量 \(\Delta\pmb{u}^{t+1}\)。代码通过 TensorFlow 自动微分（`GradientTape`）计算 Jacobian \(\partial\pmb{F}^{\mathrm{T}}/\partial\pmb{\theta}\)，与参数增量 \(\Delta\pmb{\theta}^{t+1}\) 乘积即为公式 (A.5) 的一阶近似。

---

#### 公式 (A.6)：位移增量显式表达

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 将 A.3 代入 A.5 的底层实现：Δu ≈ -η·∂F^T/∂θ·∂Π/∂θ
    @tf.function
    def Loss(self, x, y):
        with tf.GradientTape() as g:
            tmp = self.pirbn(x)
            # ... 计算损失 Π ...
            loss = l1 + l2 + l3 + l4 + l5 + l6 + l7 + l8 + l9
        # ∂Π/∂θ（即 -Δθ/η 的预处理版本）
        grads = g.gradient(loss, self.pirbn.trainable_variables)
        return loss, grads, l1, l2, l3, l4, l5, l6, l7, l8, l9

# 参数更新（对应 Δθ = -η·∂Π/∂θ）：
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)
```

**公式 (A.6)**

\[
\Delta \pmb{u}^{t+1} \approx -\eta \frac{\partial \pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial \pmb{\theta}} \frac{\partial \Pi}{\partial \pmb{\theta}}
\]

**解读**：将 \(\Delta\pmb{\theta}^{t+1} = -\eta\,\partial\Pi/\partial\pmb{\theta}\)（公式 A.3）代入 \(\Delta\pmb{u} \approx (\partial\pmb{F}^{\mathrm{T}}/\partial\pmb{\theta})\Delta\pmb{\theta}\)（公式 A.5）即得公式 (A.6)。代码中 `grads = g.gradient(loss, self.pirbn.trainable_variables)` 计算 \(\partial\Pi/\partial\pmb{\theta}\)，通过 Adam 优化步 `x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)` 实现位移增量的更新。

---

#### 公式 (A.7)：伪速度（连续极限）

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 伪速度由 Δu/η 在 η→0 极限定义；代码通过控制 learning_rate 近似该极限
        beta1 = 0.9
        beta2 = 0.999
        learning_rate = self.learning_rate    # η（学习率，趋近于 0 时对应伪速度极限）
        eps=1e-8
        ...
        for i in range(0,self.maxiter):
            loss, g = self.evaluate(x)
            m = (1 - beta1) * g + beta1 * m
            v = (1 - beta2) * (g**2) + beta2 * v
            mhat = m / (1 - beta1**(i + 1))
            vhat = v / (1 - beta2**(i + 1))
            # Δu^{t+1}/η = -∂F^T/∂θ · ∂Π/∂θ（伪速度）
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)
```

**公式 (A.7)**

\[
\frac{\partial \pmb{u}}{\partial t} = \lim_{\eta \to 0} \frac{\Delta \pmb{u}^{t+1}}{\eta}
= -\left. \frac{\partial \pmb{F}(\pmb{x};\pmb{\theta}^t)^{\mathrm{T}}}{\partial \pmb{\theta}} \frac{\partial \Pi}{\partial \pmb{\theta}} \right|_{\pmb{\theta}^t}
\]

**解读**：当学习率 \(\eta\)（`learning_rate`）趋于零时，位移增量 \(\Delta\pmb{u}^{t+1}/\eta\) 的极限即为伪速度 \(\partial\pmb{u}/\partial t\)。代码中选取足够小的 `learning_rate = 1e-5` 近似该连续极限，确保训练过程中相邻两步的位移增量不引发接触面穿透。

---

#### 公式 (A.8)：参数的梯度流

**底层计算代码**  
[`Compression_of_Two_Rubber_Rings/OPT.py`](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py)

```python
# OPT.py — 梯度流：∂θ/∂t ≈ -∂Π/∂θ（由 GradientTape 计算，经 Adam 更新实现）
    @tf.function
    def Loss(self, x, y):
        with tf.GradientTape() as g:
            tmp = self.pirbn(x)
            loss = l1 + l2 + l3 + l4 + l5 + l6 + l7 + l8 + l9
        # ∂Π/∂θ（梯度流右端项）
        grads = g.gradient(loss, self.pirbn.trainable_variables)
        return loss, grads, l1, l2, l3, l4, l5, l6, l7, l8, l9

# Adam 更新中：Δθ/η ≈ -∂Π/∂θ，对应梯度流 ∂θ/∂t ≈ -∂Π/∂θ
            x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)
```

**公式 (A.8)**

\[
\frac{\partial \pmb{\theta}}{\partial t} \approx -\frac{\partial \Pi}{\partial \pmb{\theta}}
\]

**解读**：`grads = g.gradient(loss, self.pirbn.trainable_variables)` 计算 \(\partial\Pi/\partial\pmb{\theta}\)（损失对所有可训练参数的梯度），Adam 更新步 `x = x - learning_rate * mhat / (np.sqrt(vhat) + eps)` 以自适应步长沿负梯度方向更新 \(\pmb{\theta}\)，在连续极限下等价于公式 (A.8) 的梯度流方程 \(\partial\pmb{\theta}/\partial t = -\partial\Pi/\partial\pmb{\theta}\)。

---

## 附录：代码文件索引

| 文件 | 路径 | 主要对应公式 |
|:---|:---|:---|
| `FNN.py`（Compression） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/FNN.py) | 公式 (1)、(22) |
| `PINN.py`（Compression） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/PINN.py) | 公式 (4)、(13)–(20)、推断 16/17 |
| `Dif_op.py`（Compression） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/Dif_op.py) | 推断公式 (16)（变形梯度自动微分） |
| `OPT.py`（Compression） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/OPT.py) | 公式 (2)–(3)、(5)、(11)–(12)、(A.1)–(A.8) |
| `Relax_PINN.py`（Compression） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/Relax_PINN.py) | 公式 (4)、松弛阶段（公式 5，\(E_{\mathrm{ex}}=0\)） |
| `Main.py`（Compression） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Compression_of_Two_Rubber_Rings/Main.py) | 公式 (1)、(22)（FNN 初始化） |
| `FNN.py`（Rubber_Ironing） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/FNN.py) | 公式 (6)–(10)、(22)–(24) |
| `PINN.py`（Rubber_Ironing） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/PINN.py) | 公式 (15)、(18)–(20)、推断 16/17 |
| `Dif_op.py`（Rubber_Ironing） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/Dif_op.py) | 推断公式 (16)（变形梯度自动微分） |
| `OPT.py`（Rubber_Ironing） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/OPT.py) | 公式 (7)–(8)、(11)–(12)、(21)、(A.1)–(A.8) |
| `Relax_PINN.py`（Rubber_Ironing） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/Relax_PINN.py) | 公式 (4)、松弛阶段 |
| `Main.py`（Rubber_Ironing） | [链接](https://github.com/JinshuaiBai/energy_PINN_Contact/blob/main/Rubber_Ironing/Main.py) | 公式 (24)（FNN 构建与训练循环） |
