# 公式解读

> 本文档按照论文 **Energy-based physics-informed neural network for frictionless contact problems under large deformation**（Bai et al.）的章节顺序，对论文中基本全部公式进行系统解读，对关键公式给出严格的逐步推导，并在每一推导步骤中明确所用到的数学事实或定理。所有公式均采用 LaTeX 渲染格式：显示公式用 `\[ ... \]`，行内公式和符号用 `\( ... \)`。

---

## 目录

1. [第2节：基于能量的PINN固体力学框架](#section2)
   - 2.1 势能损失函数（公式1–5）
   - 2.2 边界条件施加（公式6–10）
   - 2.3 神经网络训练与伪动力学（公式11–12）
2. [第3节：无摩擦接触势能与数值实现](#section3)
   - 3.1 接触势能（公式13–15）
   - 3.2 数值离散实现（公式18–20）
   - 3.3 松弛、渐进加载与输出缩放（公式21–22）
   - 3.4 基准测试算例（公式23–24）
3. [附录A：伪速度的严格推导（公式A.1–A.8）](#appendixA)
4. [补充：Neo-Hookean 应变能密度的完整推导](#neohookean)

---

## 第2节：基于能量的PINN固体力学框架 {#section2}

### 2.1 势能损失函数

---

#### 公式 (1)：神经网络对位移场的近似

\[
\begin{cases}
\pmb{u}(\pmb{x}) \approx \pmb{F}(\pmb{x};\,\pmb{\theta}) \\
\pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x}), \quad \pmb{x} \in \Gamma^{\pmb{u}}
\end{cases}
\tag{1}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\pmb{u}(\pmb{x})\) | 固体域 \(\Omega\) 上的位移向量场，\(\pmb{u}: \Omega \to \mathbb{R}^d\)（\(d=2\) 或 \(3\)） |
| \(\pmb{x} = (x, y)\) 或 \((x,y,z)\) | 参考（拉格朗日）坐标 |
| \(\pmb{F}(\pmb{x};\,\pmb{\theta})\) | 前馈神经网络映射，\(\pmb{F}: \mathbb{R}^d \to \mathbb{R}^d\) |
| \(\pmb{\theta}\) | 网络中所有可训练参数（权重矩阵和偏置向量）的集合 |
| \(\bar{\pmb{u}}(\pmb{x})\) | 在本质边界 \(\Gamma^{\pmb{u}}\) 上给定的位移约束值 |
| \(\Gamma^{\pmb{u}}\) | 本质边界（Dirichlet 边界），即施加位移约束的部分 |

**解读**

公式 (1) 表达了 PINN 框架的核心思路：用一个参数化的神经网络 \(\pmb{F}(\pmb{x};\pmb{\theta})\) 来近似未知的位移场 \(\pmb{u}(\pmb{x})\)。这是一个**函数近似**（function approximation）问题，神经网络充当了传统有限元法（FEM）中形函数的角色。

**数学事实**：通用近似定理（Universal Approximation Theorem）保证，对任意连续函数 \(f\)，存在足够宽或足够深的前馈神经网络能以任意精度近似 \(f\)。这为公式 (1) 提供了理论依据。

第二行约束 \(\pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x})\) 在 \(\Gamma^{\pmb{u}}\) 上是**本质边界条件**，即 Dirichlet 边界条件，是**容许位移函数空间** \(\mathcal{V}\) 的定义约束：

\[
\mathcal{V} := \left\{ \pmb{u} \in H^1(\Omega)^d \;\Big|\; \pmb{u}\big|_{\Gamma^{\pmb{u}}} = \bar{\pmb{u}} \right\}
\]

其中 \(H^1(\Omega)\) 为一阶 Sobolev 空间（位移及其一阶导数均平方可积），保证应变能积分有意义。

---

#### 公式 (2)：最小势能原理

\[
\pmb{u}^*(\pmb{x}) = \arg\min_{\pmb{u} \in \mathcal{V}} \Pi\bigl(\pmb{u}(\pmb{x})\bigr)
\tag{2}
\]

**解读**

这是**最小势能原理**（Principle of Minimum Potential Energy）的数学表述：弹性体在平衡状态时，其位移场使总势能泛函 \(\Pi\) 取极小值。

**数学事实与推导背景**：对于超弹性固体，平衡方程可以写成如下弱形式（虚功原理）：

\[
\int_\Omega \pmb{P} : \delta\pmb{F}_{\rm def}\,\mathrm{d}\Omega = \int_{\Gamma^t} \bar{\pmb{t}} \cdot \delta\pmb{u}\,\mathrm{d}\Gamma
\]

其中 \(\pmb{P}\) 是第一 Piola-Kirchhoff 应力张量，\(\pmb{F}_{\rm def} = \pmb{I} + \nabla\pmb{u}\) 是变形梯度，\(\delta\pmb{u}\) 是虚位移。对于超弹性材料，应力可由应变能密度对变形梯度求导得到：

\[
\pmb{P} = \frac{\partial \Psi}{\partial \pmb{F}_{\rm def}}
\]

将此代入虚功原理并利用变分恒等式，可知平衡位移场恰好是泛函 \(\Pi\) 的驻值点。若 \(\Psi\) 关于 \(\pmb{F}_{\rm def}\) 为**拟凸函数**（quasiconvex），则驻值点即为极小值点，公式 (2) 成立。

**实际实现**：将 \(\pmb{u} = \pmb{F}(\pmb{x};\pmb{\theta})\) 代入，最小化问题转化为关于参数 \(\pmb{\theta}\) 的优化：

\[
\pmb{\theta}^* = \arg\min_{\pmb{\theta}} \Pi\bigl(\pmb{F}(\pmb{x};\pmb{\theta})\bigr)
\]

这是一个无约束（或软约束）的有限维优化问题，可用梯度下降算法求解。

---

#### 公式 (3)：总势能泛函的分解

\[
\Pi = E_{\rm in} - E_{\rm ex}
\tag{3}
\]

**解读**

总势能由两项组成：

- \(E_{\rm in}\)：**应变能**（Strain energy），即弹性体内储存的弹性势能，总为非负。
- \(E_{\rm ex}\)：**外力势能**（Potential energy of external forces），即外力（体力或面力）做的功。

符号约定：\(-E_{\rm ex}\) 表示外力做负功（弹性体从外力"吸收"能量），故当外力对位移方向做正功时，\(E_{\rm ex} > 0\)，\(\Pi\) 减小，系统趋于平衡。

**数学事实**：\(\Pi = E_{\rm in} - E_{\rm ex}\) 是**保守系统**势能的标准分解。对线弹性问题，\(\Pi\) 关于 \(\pmb{u}\) 为严格凸泛函，保证唯一最小值的存在。对超弹性非线性问题，需额外假设（如多凸性 polyconvexity）来保证最小值的存在。

---

#### 公式 (4)：应变能

\[
E_{\rm in} = \int_\Omega \Psi(\pmb{F}_{\rm def})\,\mathrm{d}\Omega
\tag{4}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\Omega\) | 固体体域（参考构型，Lagrange 描述） |
| \(\Psi(\pmb{F}_{\rm def})\) | 应变能密度函数（Strain energy density），单位：J/m³ |
| \(\pmb{F}_{\rm def}\) | 变形梯度张量（Deformation gradient tensor） |

**变形梯度的定义**：

\[
\pmb{F}_{\rm def} = \pmb{I} + \nabla_{\pmb{X}} \pmb{u}, \qquad F_{{\rm def},ij} = \delta_{ij} + \frac{\partial u_i}{\partial X_j}
\]

其中 \(\pmb{X}\) 是参考构型坐标，\(\nabla_{\pmb{X}}\) 是对参考坐标的梯度算子。在二维情形：

\[
\pmb{F}_{\rm def} = \begin{pmatrix} 1 + \dfrac{\partial u}{\partial x} & \dfrac{\partial u}{\partial y} \\[8pt] \dfrac{\partial v}{\partial x} & 1 + \dfrac{\partial v}{\partial y} \end{pmatrix}
\]

**重要的力学量**（用于本文 Neo-Hookean 模型）：

- **Jacobian 行列式**：\(J = \det(\pmb{F}_{\rm def}) > 0\)，表示体积比（变形前后）。物理约束 \(J > 0\) 保证变形的物理可实现性（无材料穿透）。
- **右 Cauchy-Green 变形张量**：\(\pmb{C} = \pmb{F}_{\rm def}^\mathrm{T}\pmb{F}_{\rm def}\)
- **第一不变量**：\(I_1 = \mathrm{tr}(\pmb{C}) = \|\pmb{F}_{\rm def}\|_F^2 = \sum_{i,j} F_{{\rm def},ij}^2\)

**数值积分**：对公式 (4) 进行 Gauss 积分离散：

\[
E_{\rm in} \approx \sum_{k=1}^{N_{\rm int}} \Psi\!\left(\pmb{F}_{\rm def}(\pmb{x}_k)\right) w_k
\]

其中 \(\{\pmb{x}_k, w_k\}_{k=1}^{N_{\rm int}}\) 是 Gauss 积分点和相应权重。在代码中对应：

```python
l1 = tf.reduce_sum(tmp[0] * y[0])   # y[0] = w1（Gauss积分权重）
                                      # tmp[0] = Ein1（各积分点的Ψ值）
```

---

#### 公式 (5)：外力势能

\[
E_{\rm ex} = \int_{\Gamma^t} \bar{\pmb{t}} \cdot \pmb{u}\,\mathrm{d}\Gamma
\tag{5}
\]

> **注意**：原文写作 \(\int_{\Gamma^t}\bar{\pmb{t}}\cdot\mathrm{d}\Pi\)，此处 \(\mathrm{d}\Pi\) 系原文印刷错误，正确应为 \(\bar{\pmb{t}}\cdot\pmb{u}\,\mathrm{d}\Gamma\)，即面力在位移方向上所做的功。

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\Gamma^t\) | 自然边界（Neumann 边界），即施加面力的部分 |
| \(\bar{\pmb{t}}\) | 给定的边界面力向量（traction force），单位：Pa |
| \(\pmb{u}\) | 该边界上的位移向量 |

**解读**

\(E_{\rm ex}\) 表示面力 \(\bar{\pmb{t}}\) 在位移场 \(\pmb{u}\) 上所做的功，属于**线性泛函**（对 \(\pmb{u}\) 是线性的）。对于体力 \(\pmb{b}\)（单位体积力），还需加上：

\[
E_{\rm body} = \int_\Omega \pmb{b} \cdot \pmb{u}\,\mathrm{d}\Omega
\]

**数学事实**：自然边界条件（Neumann BC）\(\pmb{P}\cdot\pmb{n} = \bar{\pmb{t}}\) 自动嵌入到泛函 \(\Pi\) 的变分之中，不需要额外处理——这是**能量方法**相对于强形式方法（需要显式施加 Neumann BC）的一大优势。

验证：对 \(\Pi = E_{\rm in} - E_{\rm ex}\) 关于 \(\pmb{u}\) 求一阶变分（Gâteaux 导数），令其为零：

\[
\delta\Pi = \int_\Omega \frac{\partial\Psi}{\partial\pmb{F}_{\rm def}}:\delta\pmb{F}_{\rm def}\,\mathrm{d}\Omega - \int_{\Gamma^t}\bar{\pmb{t}}\cdot\delta\pmb{u}\,\mathrm{d}\Gamma = 0
\]

注意到 \(\delta\pmb{F}_{\rm def} = \nabla\delta\pmb{u}\) 以及 \(\pmb{P} = \partial\Psi/\partial\pmb{F}_{\rm def}\)，利用分部积分（散度定理）：

\[
\int_\Omega \pmb{P}:\nabla\delta\pmb{u}\,\mathrm{d}\Omega = -\int_\Omega (\mathrm{Div}\,\pmb{P})\cdot\delta\pmb{u}\,\mathrm{d}\Omega + \int_{\partial\Omega}(\pmb{P}\cdot\pmb{n})\cdot\delta\pmb{u}\,\mathrm{d}\Gamma
\]

其中 \(\mathrm{Div}\,\pmb{P}\) 是对参考坐标的散度。令 \(\delta\Pi = 0\) 对所有容许 \(\delta\pmb{u}\) 成立，即得强形式平衡方程（Euler-Lagrange 方程）：

\[
\mathrm{Div}\,\pmb{P} + \pmb{b} = \pmb{0}, \quad \text{在 } \Omega \text{ 内}
\]
\[
\pmb{P}\cdot\pmb{n} = \bar{\pmb{t}}, \quad \text{在 } \Gamma^t \text{ 上}
\]

这证明了能量泛函的稳定点恰好满足平衡方程和自然边界条件。

---

### 2.2 边界条件施加

---

#### 公式 (6)：本质边界条件

\[
\pmb{u}(\pmb{x}) = \bar{\pmb{u}}(\pmb{x}), \qquad \pmb{x} \in \Gamma^{\pmb{u}}
\tag{6}
\]

**解读**

这是固体力学中的 **Dirichlet 边界条件**（第一类边界条件），规定了边界 \(\Gamma^{\pmb{u}}\) 上的位移值。对于固定边界，\(\bar{\pmb{u}} = \pmb{0}\)；对于位移加载边界，\(\bar{\pmb{u}} \neq \pmb{0}\)。

与自然边界条件（公式5中的面力 \(\bar{\pmb{t}}\)）不同，本质边界条件**不会自动被能量泛函的极值条件满足**，必须显式施加。

---

#### 公式 (7)：软边界条件施加——罚函数方法

\[
\Pi_{\rm EBC} = \int_{\Gamma^{\pmb{u}}} \|\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\|_2^2\,\mathrm{d}\Gamma
= \int_{\Gamma^{\pmb{u}}} \bigl[\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\bigr] \cdot \bigl[\pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x})\bigr]\,\mathrm{d}\Gamma
\tag{7}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\Pi_{\rm EBC}\) | 本质边界条件残差泛函（Essential Boundary Condition residual） |
| \(\|\cdot\|_2\) | Euclidean \(\ell^2\) 范数 |

**严格推导**

步骤1：定义边界残差向量

\[
\pmb{e}(\pmb{x}) := \pmb{u}(\pmb{x}) - \bar{\pmb{u}}(\pmb{x}), \qquad \pmb{x} \in \Gamma^{\pmb{u}}
\]

步骤2：计算残差的 \(L^2\) 范数的平方（边界积分）

\[
\Pi_{\rm EBC} = \int_{\Gamma^{\pmb{u}}} \|\pmb{e}(\pmb{x})\|_2^2\,\mathrm{d}\Gamma = \int_{\Gamma^{\pmb{u}}} \pmb{e}(\pmb{x}) \cdot \pmb{e}(\pmb{x})\,\mathrm{d}\Gamma
\]

步骤3：展开点积（对二维问题 \(\pmb{u}=(u,v)\)，\(\bar{\pmb{u}}=(\bar{u},\bar{v})\)）

\[
\Pi_{\rm EBC} = \int_{\Gamma^{\pmb{u}}} \left[(u - \bar{u})^2 + (v - \bar{v})^2\right]\mathrm{d}\Gamma
\]

**数学事实**：\(\Pi_{\rm EBC} \geq 0\) 恒成立（非负性）；且 \(\Pi_{\rm EBC} = 0 \iff \pmb{u} = \bar{\pmb{u}}\) a.e. 在 \(\Gamma^{\pmb{u}}\) 上（正定性）。这是因为被积函数为完全平方形式。

**数值离散**：离散化后用 \(n_b\) 个边界采样点：

\[
\Pi_{\rm EBC} \approx \frac{1}{n_b}\sum_{k=1}^{n_b} \|\pmb{u}(\pmb{x}_k^b) - \bar{\pmb{u}}(\pmb{x}_k^b)\|_2^2
\]

在代码中对应（`OPT.py`）：

```python
l4 = tf.reduce_mean(tf.square(tmp[3] + iter))
# tmp[3] = V1_t（预测的顶面竖向位移），iter = 目标位移（取负值方向）
```

---

#### 公式 (8)：修正势能泛函（软边界）

\[
\Pi^* = \Pi + \kappa\,\Pi_{\rm EBC}
\tag{8}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\Pi^*\) | 加入惩罚项后的修正泛函 |
| \(\kappa > 0\) | 惩罚因子（penalty factor） |

**严格分析**

设 \(\pmb{u}_\kappa\) 为修正泛函 \(\Pi^*\) 的极小值点，则：

\[
\Pi^*(\pmb{u}_\kappa) \leq \Pi^*(\pmb{u}^*) = \Pi(\pmb{u}^*) + \kappa \cdot 0 = \Pi(\pmb{u}^*)
\]

另一方面，由于 \(\kappa\Pi_{\rm EBC}(\pmb{u}_\kappa) \geq 0\)：

\[
\Pi(\pmb{u}_\kappa) \leq \Pi^*(\pmb{u}_\kappa) \leq \Pi(\pmb{u}^*)
\]

由此可得 \(\Pi_{\rm EBC}(\pmb{u}_\kappa) = \frac{1}{\kappa}[\Pi^*(\pmb{u}_\kappa) - \Pi(\pmb{u}_\kappa)] \leq \frac{1}{\kappa}\Pi(\pmb{u}^*)\)，当 \(\kappa \to +\infty\) 时 \(\Pi_{\rm EBC}(\pmb{u}_\kappa) \to 0\)，即 \(\pmb{u}_\kappa \to \pmb{u}^*\)。

**数学事实**：这是**罚函数法**（Penalty method）的收敛性定理。在有限元法中该方法早有应用（参见 Barrett & Elliott, 1986 [59]）。

**实践注意**：\(\kappa\) 太小会导致边界条件满足程度差；\(\kappa\) 太大会使 Hessian 矩阵（或梯度信息）呈现病态，导致优化困难。本文取 \(\kappa = 10^4\)。

---

#### 公式 (9)：硬边界条件施加

\[
\pmb{u}(\pmb{x}) = \pmb{F}(\pmb{x}) \odot \pmb{g}(\pmb{x}) + \bar{\pmb{u}}(\pmb{x})
\tag{9}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\pmb{F}(\pmb{x})\) | 神经网络原始输出（无约束的自由输出） |
| \(\odot\) | 逐元素（Hadamard）乘积 |
| \(\pmb{g}(\pmb{x})\) | 距离函数（distance function），定义见公式 (10) |
| \(\bar{\pmb{u}}(\pmb{x})\) | 边界上的给定位移值，在域内适当延拓 |

**严格验证边界条件自动满足**

对任意 \(\pmb{x}_0 \in \Gamma^{\pmb{u}}\)：

\[
\pmb{g}(\pmb{x}_0) = 0 \quad \Longrightarrow \quad \pmb{u}(\pmb{x}_0) = \pmb{F}(\pmb{x}_0) \odot \pmb{0} + \bar{\pmb{u}}(\pmb{x}_0) = \bar{\pmb{u}}(\pmb{x}_0)
\]

无论网络参数 \(\pmb{\theta}\) 取何值，边界条件 \(\pmb{u}|_{\Gamma^{\pmb{u}}} = \bar{\pmb{u}}\) 精确成立。这是"硬"边界的核心机制：利用距离函数在边界处恒为零的性质，将边界条件嵌入网络结构。

**域内行为**：当 \(\pmb{x}\) 远离 \(\Gamma^{\pmb{u}}\) 时，\(\pmb{g}(\pmb{x}) > 0\)，网络输出 \(\pmb{F}(\pmb{x})\) 有充分的自由度来逼近内部位移场；\(\bar{\pmb{u}}(\pmb{x})\) 在内部起一个初始猜测或背景位移场的作用。

**与软边界的比较**：

| 性质 | 软边界（公式8） | 硬边界（公式9） |
|:---|:---|:---|
| 边界条件满足程度 | 近似满足（残差 \(\propto 1/\kappa\)） | 精确满足 |
| 损失项数 | 需要额外惩罚项 | 不需要额外损失 |
| 超参数 | 需要调 \(\kappa\) | 不需要 |
| 复杂边界 | 容易实现 | 需构造距离函数 |

**代码实现**（`FNN.py`，`time_layer` 类）：

```python
class time_layer(tf.keras.layers.Layer):
    def __init__(self, variable):
        # variable = 边界坐标值（x_boundary 或 y_boundary）
        self.variable = variable

    def call(self, inputs, x):
        # inputs：网络原始输出 F(x)
        # x：坐标分量（x 轴或 y 轴坐标值）
        # 返回：F(x) * (x_coord - x_boundary)
        # 当 x_coord = x_boundary 时自动为零
        return inputs * (x - self.variable)
```

对于橡皮熨烫问题（公式24），距离函数 \(\pmb{g}(\pmb{x})\) 取点到边界的有符号距离 \(y - y_{\rm boundary}\)：
- 半圆柱顶边 \(y=1\)：\(g_c(y) = y - 1\)，保证 \(U_c(y=1) = U_t, V_c(y=1) = V_t\)
- 平板底边 \(y=-1.5\)：\(g_s(y) = y - (-1.5) = y + 1.5\)，保证底边位移为零

---

#### 公式 (10)：距离函数的定义

\[
\pmb{g}(\pmb{x}) = \min_{\pmb{y}\in \Gamma^{\pmb{u}}} \|\pmb{x} - \pmb{y}\|_2^2 = \min_{\pmb{y}\in \Gamma^{\pmb{u}}} \left[(\pmb{x}-\pmb{y})\cdot(\pmb{x}-\pmb{y})\right]
\tag{10}
\]

**解读**

公式 (10) 给出了距离函数的**一般形式**：点 \(\pmb{x}\) 到本质边界 \(\Gamma^{\pmb{u}}\) 的最短距离的平方（严格来说是距离的平方，这与通常定义的距离函数稍有不同，但在数值实现中只需要边界处为零这一性质）。

**性质**：
1. \(\pmb{g}(\pmb{x}) = 0\) 当且仅当 \(\pmb{x} \in \Gamma^{\pmb{u}}\)
2. \(\pmb{g}(\pmb{x}) > 0\) 当 \(\pmb{x} \notin \Gamma^{\pmb{u}}\)
3. \(\pmb{g}\) 在 \(\Gamma^{\pmb{u}}\) 的邻域内是光滑的（对简单几何），适合与神经网络复合

**简单几何的显式形式**：

对于矩形域，边界 \(x = 0\)（固定边）：\(g(x,y) = x\)

对于边界 \(y = y_0\)（顶面或底面）：\(g(x,y) = |y - y_0|\) 或 \((y-y_0)\)（有符号距离）

本文在代码中直接使用有符号线性距离，如 \(g(y) = y - y_{\rm top}\)。

**数学事实**：从数学分析的角度，\(\pmb{g}(\pmb{x})\) 实际上是 \(\pmb{x}\) 到闭集 \(\Gamma^{\pmb{u}}\) 的平方距离函数，它是全域 Lipschitz 连续且在所有 \(\pmb{x} \notin \Gamma^{\pmb{u}}\) 处可微（当 \(\Gamma^{\pmb{u}}\) 光滑时）。

---

### 2.3 神经网络训练与伪动力学

---

#### 公式 (11)：训练过程的伪速度

\[
\frac{\partial \pmb{u}}{\partial t} \approx -\frac{\partial \pmb{F}(\pmb{x};\pmb{\theta})^\mathrm{T}}{\partial \pmb{\theta}} \frac{\partial \Pi}{\partial \pmb{\theta}}
\tag{11}
\]

**解读**

这是本文的重要理论结论之一：神经网络训练过程中，位移场的变化率（"伪速度"）由网络雅可比矩阵（\(\partial\pmb{F}/\partial\pmb{\theta}\)）与势能梯度（\(\partial\Pi/\partial\pmb{\theta}\)）的乘积决定。完整推导见附录 A（公式 A.1–A.8）。

**物理意义**：训练过程等价于将弹性体在"伪时间"中从初始状态（随机位移场）演化到平衡状态（势能极小点），类似于粘弹性动力学中的能量耗散过程。

---

#### 公式 (12)：位移增量的估计

\[
\Delta\pmb{u} \approx \eta\,\frac{\partial \pmb{u}}{\partial t}
\tag{12}
\]

**解读**

这是公式 (11) 的直接应用：在一步梯度下降（步长 \(\eta\)）中，位移场的增量约等于步长乘以伪速度。

**推导**：由公式 (A.6)（见附录 A 推导部分）：

\[
\Delta\pmb{u}^{t+1} \approx -\eta\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}
= \eta\left(-\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}\right)
= \eta\frac{\partial\pmb{u}}{\partial t}
\]

**工程应用**：公式 (12) 提供了一个**先验估计**，可据此选取合适的学习率 \(\eta\)：若希望每步位移增量 \(\Delta\pmb{u}\) 不超过某个阈值（如接触点间距的某个比例），则应满足：

\[
\eta \leq \frac{\Delta u_{\max}}{\left|\partial u/\partial t\right|}
\]

这样可避免接触面在单步训练中发生穿透。

---

## 第3节：无摩擦接触势能与数值实现 {#section3}

### 3.1 无摩擦接触势能

---

#### 关于 Lennard-Jones 势（背景知识，论文公式16–17的隐含内容）

论文提到接触势受 **Lennard-Jones（LJ）势**启发。LJ 势是描述两分子间相互作用的经典势函数：

\[
V_{\rm LJ}(r) = 4\varepsilon\left[\left(\frac{\sigma}{r}\right)^{12} - \left(\frac{\sigma}{r}\right)^6\right]
\]

其中 \(\varepsilon\) 是势阱深度，\(\sigma\) 是分子直径，\(r\) 是分子间距。

特征：
- 当 \(r \to 0\) 时，\(V_{\rm LJ} \to +\infty\)（强排斥，防止重叠）
- 当 \(r = 2^{1/6}\sigma\) 时，\(V_{\rm LJ}\) 取极小值 \(-\varepsilon\)（平衡位置）
- 当 \(r \to \infty\) 时，\(V_{\rm LJ} \to 0\)（远程衰减）

LJ 势不是单调函数，具有吸引-排斥双重性。对于宏观接触问题，吸引区域物理上不出现（宏观物体不会像分子那样"粘合"），因此可用**单调**近似代替。

---

#### 公式 (13)：含接触势的总势能

\[
\Pi = E_{\rm in} - E_{\rm ex} + E_{\rm c}
\tag{13}
\]

**解读**

在原始势能 \(\Pi = E_{\rm in} - E_{\rm ex}\)（公式3）的基础上，增加了**接触势能** \(E_{\rm c}\)。

**物理意义**：\(E_{\rm c}\) 是一个**排斥势能项**，当两接触面靠近时，\(E_{\rm c}\) 迅速增大，为最小化 \(\Pi\) 的优化过程提供了阻止穿透的"驱动力"。

**与传统方法的对比**：传统接触力学将不穿透约束表述为不等式约束 \(g_N \leq 0\)（法向间隙非负），需要专门的约束处理算法（Lagrange 乘子、增广 Lagrange 法等）。本文将此约束"软化"为势能罚项，使整个问题归结为单一的无约束（或软约束）优化，大大简化了实现。

---

#### 公式 (14)：接触势能的面积分表达式

\[
E_{\rm c} = \int_{\Gamma_1}\int_{\Gamma_2} \beta_1\,\beta_2\,\phi(r)\,\mathrm{d}\Gamma_1\,\mathrm{d}\Gamma_2
\tag{14}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\Gamma_1, \Gamma_2\) | 两个固体的潜在接触面（在参考构型中描述） |
| \(\beta_1, \beta_2\) | 两个接触面的面密度（surface density），用于将面积分转换为离散求和 |
| \(\phi(r)\) | 接触势函数（contact potential function） |
| \(r = r(\pmb{x}_1, \pmb{x}_2)\) | 接触面上点 \(\pmb{x}_1 \in \Gamma_1\) 与点 \(\pmb{x}_2 \in \Gamma_2\) 在**当前构型**中的距离 |

**物理推导**

设两面上的表面密度分别为 \(\beta_1, \beta_2\)（类比分子密度），则接触面上微元 \(\mathrm{d}\Gamma_1\) 包含 \(\beta_1\,\mathrm{d}\Gamma_1\) 个"等效粒子"，微元 \(\mathrm{d}\Gamma_2\) 包含 \(\beta_2\,\mathrm{d}\Gamma_2\) 个粒子。两微元间的总相互作用势为：

\[
\mathrm{d}E_{\rm c} = (\beta_1\,\mathrm{d}\Gamma_1)(\beta_2\,\mathrm{d}\Gamma_2)\,\phi(r(\pmb{x}_1,\pmb{x}_2))
\]

对全接触面积分得公式 (14)。这是 Sauer & De Lorenzis（2013）[63] 提出的接触势能框架。

**数学性质**：

1. \(E_{\rm c} \geq 0\)（因 \(\phi(r) \geq 0\)）
2. 当所有接触面距离 \(r \to \infty\) 时，\(E_{\rm c} \to 0\)（无接触无势能）
3. 当两面靠近 \(r \to 0\) 时，\(E_{\rm c} \to +\infty\)（强排斥，防穿透）
4. \(E_{\rm c}\) 对当前构型坐标可微，从而可用自动微分计算梯度

---

#### 公式 (15)：指数型接触势函数

\[
\phi(r) = \phi_0\,\exp\!\left(-\frac{r}{r_0}\right)
\tag{15}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\phi_0 > 0\) | 势能幅值参数（对应 \(r=0\) 时的势能高度） |
| \(r_0 > 0\) | 特征长度（衰减半径），控制势能随距离的衰减速率 |
| \(r\) | 两接触点当前构型下的 Euclidean 距离 |

**性质验证**

1. \(\phi(r) > 0\) 对所有 \(r \geq 0\)（始终为正，纯排斥）
2. \(\phi'(r) = -\frac{\phi_0}{r_0}\exp(-r/r_0) < 0\)（单调递减，距离越小势能越大）
3. \(\phi(0) = \phi_0\)（有限高度，不发散）
4. \(\lim_{r\to\infty}\phi(r) = 0\)（远程截断）
5. \(\phi(r)\) 是光滑函数，对 \(r\) 无限次可微

**与 LJ 势的比较**：本文选取指数型势而非 LJ 势，主要考虑：
- LJ 势在 \(r \to 0\) 时发散（\(\propto r^{-12}\)），数值上不稳定
- LJ 势有吸引区间（对宏观接触无物理意义）
- 指数型势数学性质更好（有界、光滑），且在宏观尺度足够近似 LJ 势的排斥行为

**参数物理含义**：
- \(r_0\) 类比于分子动力学中的粒子间距，减小 \(r_0\) 等效于增加接触面采样密度，可提升接触区精度
- \(\phi_0\) 类比于罚函数法中的罚因子 \(\kappa\)，过小会导致穿透，过大不影响结果精度但可能影响训练稳定性

---

### 3.2 数值离散实现

---

#### 公式 (18)：接触点距离矩阵

\[
r_{n\times m} = \sqrt{(\Delta\pmb{u})^2 + (\Delta\pmb{v})^2}
\tag{18}
\]

> **注**：论文公式编号从 (15) 直接跳到 (18)，说明公式 (16) 和 (17) 在原始论文中可能是图注公式或 LJ 势相关推导，在本文档来源的 .md 文件中未出现。

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(n\) | 接触面 \(\Gamma_1\) 上的采样点数 |
| \(m\) | 接触面 \(\Gamma_2\) 上的采样点数 |
| \(\Delta\pmb{u}_{n\times m}\) | 水平坐标差矩阵（公式19第一式） |
| \(\Delta\pmb{v}_{n\times m}\) | 竖向坐标差矩阵（公式19第二式） |
| \(r_{n\times m}\) | \(n\times m\) 的距离矩阵，第 \((i,j)\) 元素是面1第 \(i\) 点与面2第 \(j\) 点在当前构型中的 Euclidean 距离 |

**解读**

这里的 \(\Delta\pmb{u}, \Delta\pmb{v}\) 是当前构型下的坐标差，即：

\[
\Delta u_{ij} = x_i^{(1)} - x_j^{(2)}, \qquad \Delta v_{ij} = y_i^{(1)} - y_j^{(2)}
\]

其中 \((x_i^{(1)}, y_i^{(1)})\) 是面1第 \(i\) 点在当前构型中的坐标，\((x_j^{(2)}, y_j^{(2)})\) 是面2第 \(j\) 点在当前构型中的坐标。

**当前构型坐标的计算**：

\[
x_i^{(1)} = X_i^{(1)} + u_i^{(1)}, \qquad y_i^{(1)} = Y_i^{(1)} + v_i^{(1)}
\]

其中 \((X_i^{(1)}, Y_i^{(1)})\) 是参考坐标，\((u_i^{(1)}, v_i^{(1)})\) 是神经网络预测的位移。

**代码实现**（`PINN.py`）：

```python
# 计算接触面当前构型坐标
U1_cont = fu1(x1_cont) + x1_cont[..., 0, tf.newaxis]  # x1 + u1（水平坐标）
V1_cont = fv1(x1_cont) + x1_cont[..., 1, tf.newaxis] + 2.0  # y1 + v1 + 初始偏置
U2_cont = fu2(x2_cont) + x2_cont[..., 0, tf.newaxis]
V2_cont = fv2(x2_cont) + x2_cont[..., 1, tf.newaxis] + 1.5

# 计算距离矩阵（利用TF广播实现外积）
dU = U1_cont * cost1 - tf.transpose(U2_cont * cost2)  # Δu_{n×m}
dV = V1_cont * cost1 - tf.transpose(V2_cont * cost2)  # Δv_{n×m}
r  = tf.sqrt(tf.square(dU) + tf.square(dV))            # r_{n×m}
```

---

#### 公式 (19)：坐标差矩阵的外积公式

\[
\begin{cases}
\Delta\pmb{u}_{n\times m} = \pmb{x}_{n\times 1} \otimes \pmb{1}_{m\times 1} - \left(\pmb{x}_{m\times 1} \otimes \pmb{1}_{n\times 1}\right)^\mathrm{T} \\[6pt]
\Delta\pmb{v}_{n\times m} = \pmb{y}_{n\times 1} \otimes \pmb{1}_{m\times 1} - \left(\pmb{y}_{m\times 1} \otimes \pmb{1}_{n\times 1}\right)^\mathrm{T}
\end{cases}
\tag{19}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\pmb{x}_{n\times 1}\) | 面1上 \(n\) 个接触点的当前 \(x\) 坐标列向量 |
| \(\pmb{x}_{m\times 1}\) | 面2上 \(m\) 个接触点的当前 \(x\) 坐标列向量 |
| \(\pmb{1}_{m\times 1}\) | 全1列向量，长度为 \(m\) |
| \(\otimes\) | 外积（outer product）：\(\pmb{a}_{n\times 1} \otimes \pmb{b}_{m\times 1} = \pmb{a}\pmb{b}^\mathrm{T}\)，结果为 \(n\times m\) 矩阵 |

**严格推导**

利用外积展开：

\[
\pmb{x}_{n\times 1} \otimes \pmb{1}_{m\times 1} = \pmb{x}\pmb{1}^\mathrm{T} = \begin{pmatrix} x_1 & x_1 & \cdots & x_1 \\ x_2 & x_2 & \cdots & x_2 \\ \vdots & & & \vdots \\ x_n & x_n & \cdots & x_n \end{pmatrix}_{n\times m}
\]

\[
\left(\pmb{x}_{m\times 1} \otimes \pmb{1}_{n\times 1}\right)^\mathrm{T} = \left(\pmb{x}'\pmb{1}^\mathrm{T}\right)^\mathrm{T} = \pmb{1}\pmb{x}'^{\mathrm{T}} = \begin{pmatrix} x_1' & x_2' & \cdots & x_m' \\ x_1' & x_2' & \cdots & x_m' \\ \vdots & & & \vdots \\ x_1' & x_2' & \cdots & x_m' \end{pmatrix}_{n\times m}
\]

其中 \(x_j'\) 是面2第 \(j\) 个点的 \(x\) 坐标。相减得：

\[
(\Delta\pmb{u})_{ij} = x_i - x_j' \qquad (i=1,\ldots,n;\; j=1,\ldots,m)
\]

这正是面1第 \(i\) 点与面2第 \(j\) 点的水平坐标差。

**数学事实**：外积（outer product）是张量积（tensor product）的一个具体实例，在此用来通过向量运算（一维）生成矩阵（二维），避免了显式的双重循环，高效利用 GPU/CPU 的矩阵运算能力。

**TensorFlow 实现**：由于 TensorFlow 支持广播（broadcasting），代码中利用形状广播隐式实现外积：

```python
# x1_cont.shape = (n, 2), x2_cont.shape = (m, 2)
# cost1.shape = (n, 1), cost2.shape = (m, 1)
# U1_cont * cost1 的形状为 (n, 1)
# tf.transpose(U2_cont * cost2) 的形状为 (1, m)
# 相减时自动广播，得到 (n, m) 的差矩阵
dU = U1_cont * cost1 - tf.transpose(U2_cont * cost2)
```

---

#### 公式 (20)：接触势矩阵的计算

\[
\phi(r)_{n\times m} = \phi_0\,\exp\!\left(-\frac{r_{n\times m}}{r_0}\right)
\tag{20}
\]

**解读**

将公式 (18) 得到的距离矩阵 \(r_{n\times m}\) 代入公式 (15) 的指数势函数，得到 \(n\times m\) 的势能矩阵，其第 \((i,j)\) 元素是面1第 \(i\) 点与面2第 \(j\) 点的相互作用势。

**与公式 (14) 的联系（数值积分化）**

将双重面积分离散：

\[
E_{\rm c} = \int_{\Gamma_1}\int_{\Gamma_2}\beta_1\beta_2\,\phi(r)\,\mathrm{d}\Gamma_1\,\mathrm{d}\Gamma_2 \approx \sum_{i=1}^n\sum_{j=1}^m \phi(r_{ij})\,\Delta\Gamma_{1,i}\,\Delta\Gamma_{2,j}
\]

其中 \(\Delta\Gamma_{1,i} = \beta_1 \cdot \mathrm{ds}_{1,i}\) 是面1第 \(i\) 个离散点的弧长权重，代码中对应 `cost1`/`dr1`。

**代码实现**（`PINN.py`）：

```python
a = 1e2   # = 1/r_0
b = 1e2   # = φ_0
Erepulsive = b * tf.exp(-a * r)   # φ(r)_{n×m}
```

**损失中的求和**（`OPT.py`）：

```python
l3 = tf.reduce_sum(tmp[2] * y[2] * y[2])
# tmp[2] = Erepulsive（n×m矩阵）
# y[2] = dr1（弧长权重向量，相当于 ΔΓ）
# y[2] * y[2] = ΔΓ_1 · ΔΓ_2（两面弧长权重的外积）
# l3 ≈ E_c
```

**重要性质**：整个计算图（参数 \(\pmb{\theta}\) → 位移 \(\pmb{u}\) → 当前构型坐标 → 距离矩阵 \(r\) → 势能矩阵 \(\phi(r)\) → \(E_c\)）均可被 TensorFlow 的自动微分引擎追踪，无需手工推导梯度。

---

### 3.3 松弛、渐进加载与输出缩放

---

#### 公式 (21)：渐进加载的边界条件惩罚泛函

\[
\Pi_{\rm EBC}(t) = \frac{1}{n}\left\|\pmb{u}(\pmb{x}) - \frac{t}{t_{\max}}\bar{\pmb{u}}(\pmb{x})\right\|_2^2
\tag{21}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(t\) | 当前训练迭代步数（"伪时间"） |
| \(t_{\max}\) | 最大训练步数 |
| \(\bar{\pmb{u}}(\pmb{x})\) | 目标位移（加载终态） |
| \(n\) | 本质边界上的采样点数 |
| \(t/t_{\max} \in [0,1]\) | 时间归一化因子 |

**严格解读**

公式 (21) 是公式 (7) 的时间参数化扩展。在第 \(t\) 步，目标位移被缩放为 \(\frac{t}{t_{\max}}\bar{\pmb{u}}\)：

- \(t = 0\)：目标位移为零，强制 \(\pmb{u} \approx \pmb{0}\)（松弛到自然状态）
- \(t = t_{\max}\)：目标位移为满载荷 \(\bar{\pmb{u}}\)（达到最终加载状态）
- 中间步：目标位移线性增长，实现**渐进加载**

**数学意义**：这等价于在每个训练步将本质边界条件更新为：

\[
\bar{\pmb{u}}^{(t)} = \frac{t}{t_{\max}}\bar{\pmb{u}}
\]

训练目标 \(\Pi_{\rm EBC}(t) \to 0\) 意味着预测位移趋近于 \(t\) 时刻对应的目标位移。

**与伪动力学的联系**：渐进加载与神经网络训练的伪动力学（公式11–12）相结合，保证了每步位移增量不超过接触间距，从而避免穿透：

\[
\left|\Delta u_{\rm BC}^{(t)}\right| = \left|\frac{1}{t_{\max}}\bar{u}\right| \ll r_0
\]

**代码实现**（`OPT.py`）：

```python
# 加载阶段（t_or_r = 0）
ttt = tf.constant((self.i - 1) * 0.1 + self.iter * 0.1 * 0.5e-3)
# self.i：加载步序号，self.iter：当前步内迭代次数
# 目标位移随迭代线性增大

# 损失中对应 Π_EBC(t)
l4 = tf.reduce_mean(tf.square(tmp[3] + iter))
# tmp[3] = V1_t（预测竖向位移），iter = ttt（目标位移，负方向）
```

---

#### 公式 (22)：含输出缩放的硬边界条件网络结构

\[
\pmb{u}(\pmb{x}) = \xi\left(\pmb{F}(\pmb{x}) \odot \pmb{g}(\pmb{x}) + \bar{\pmb{u}}(\pmb{x})\right)
\tag{22}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(\xi\) | 输出缩放因子（pre-defined scaling factor），量纲与位移相同 |

**解读**

这是公式 (9) 的扩展，在外层添加缩放因子 \(\xi\)。

**引入缩放的必要性**：

神经网络的 LeCun 初始化和 tanh 激活函数的组合，使得网络输出天然集中在 \([-1,1]\) 区间。若真实位移场量级为 \(10^{-3}\) m（如 Hertz 接触中的微小变形），则网络输出需要从 \(\mathcal{O}(1)\) 映射到 \(\mathcal{O}(10^{-3})\)，全靠最后一层权重来实现缩放，导致这一层权重极小，梯度难以有效传播（梯度消失问题的一种表现）。

**解决方案**：引入显式缩放因子 \(\xi\)，令网络输出 \(\pmb{F}\) 仍在 \(\mathcal{O}(1)\) 量级，再乘以 \(\xi\) 得到物理量级的位移。

**验证边界条件仍然满足**：对 \(\pmb{x}_0 \in \Gamma^{\pmb{u}}\)，\(\pmb{g}(\pmb{x}_0) = 0\)：

\[
\pmb{u}(\pmb{x}_0) = \xi\bigl(\pmb{F}(\pmb{x}_0)\odot\pmb{0} + \bar{\pmb{u}}(\pmb{x}_0)\bigr) = \xi\,\bar{\pmb{u}}(\pmb{x}_0)
\]

注意：若 \(\bar{\pmb{u}}(\pmb{x}_0) = 0\)（固定边界），则边界条件精确满足；若 \(\bar{\pmb{u}} \neq 0\)，则实际施加的边界值为 \(\xi\bar{\pmb{u}}\)，因此 \(\xi\) 和 \(\bar{\pmb{u}}\) 需要配合使用（或在 \(\bar{\pmb{u}}\) 中除以 \(\xi\)）。

**Hertz 接触中的具体取值**：\(\xi = 10^{-3}\)，对应微小变形。

---

### 3.4 基准测试算例

---

#### 公式 (23)：Hertz 接触问题的神经网络输出构造

\[
\begin{cases}
U = \xi\,x\,F_x(x, y) \\
V = \xi\,F_y(x, y)
\end{cases}
\tag{23}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| \(U, V\) | 水平和竖向位移预测值 |
| \(F_x(x,y), F_y(x,y)\) | 两个独立前馈神经网络的输出（无约束） |
| \(\xi = 10^{-3}\) | 输出缩放因子 |

**硬边界条件的体现**：

- \(U = \xi\,x\,F_x(x,y)\)：当 \(x=0\)（对称轴）时，\(U = 0\)。这利用了半圆柱关于 \(x=0\) 的对称性，水平位移在对称轴上为零，通过乘以 \(x\) 实现。
- \(V\) 没有显式的硬边界条件（竖向位移在各边界值通过软边界或其他方式处理）。

**与公式 (22) 的联系**：公式 (23) 是公式 (22) 的 Hertz 问题特化版本：
- 对 \(U\)：\(\pmb{g}(\pmb{x}) = x\)（到 \(x=0\) 的距离），\(\bar{u}=0\)
- 对 \(V\)：仅有缩放，无硬边界

---

#### 公式 (24)：橡皮熨烫问题的神经网络输出构造

\[
\begin{cases}
U_{\rm c} = (y-3)\,u_{\rm c}(x,y) + U_t \\
V_{\rm c} = (y-3)\,v_{\rm c}(x,y) + V_t \\
U_{\rm s} = y\,u_{\rm s}(x,y) \\
V_{\rm s} = y\,v_{\rm s}(x,y)
\end{cases}
\tag{24}
\]

**符号说明**

| 符号 | 含义 |
|:---|:---|
| 下标 c | 半圆柱（cylinder） |
| 下标 s | 平板（slab） |
| \(u_{\rm c}, v_{\rm c}, u_{\rm s}, v_{\rm s}\) | 四个独立前馈神经网络的原始输出 |
| \(U_t, V_t\) | 当前加载步的目标位移（随加载步更新） |
| \(y-3\) | 半圆柱顶边 \(y=3\) 到内部点的距离函数 |
| \(y\) | 平板底边 \(y=0\) 到内部点的距离函数 |

**逐行解读**

**第一行**：\(U_{\rm c} = (y-3)\,u_{\rm c}(x,y) + U_t\)

这是公式 (9) 的具体实例，其中 \(\pmb{g}(\pmb{x}) = y-3\)（到顶边的有符号距离），\(\bar{u} = U_t\)（顶边施加的水平目标位移）。

在顶边 \(y=3\) 处：\(U_{\rm c} = 0 \cdot u_c + U_t = U_t\) ✓

**第二行**：\(V_{\rm c} = (y-3)\,v_{\rm c}(x,y) + V_t\)

类似地，在顶边 \(y=3\) 处：\(V_{\rm c} = V_t\) ✓（施加竖向压入位移）

**第三行**：\(U_{\rm s} = y\,u_{\rm s}(x,y)\)

平板底边位于 \(y=0\)，距离函数 \(g(y) = y - 0 = y\)，\(\bar{u}_s = 0\)（底边水平固定）。

在底边 \(y=0\)：\(U_{\rm s} = 0\) ✓

**第四行**：\(V_{\rm s} = y\,v_{\rm s}(x,y)\)

在底边 \(y=0\)：\(V_{\rm s} = 0\) ✓（底边竖向固定）

**小结**：公式 (24) 用4个独立神经网络分别预测两个固体的两个位移分量，每个网络的输出结构都按公式 (9) 的模板构造，确保所有本质边界条件在训练全程精确满足。

---

## 附录A：伪速度的严格推导 {#appendixA}

附录 A 提供了公式 (11) 和 (12) 的完整推导链。以下给出每步的数学事实与步骤解析。

---

#### 公式 (A.1)：第 \(t\) 步前的位移预测

\[
\pmb{u}^t = \pmb{F}(\pmb{x};\,\pmb{\theta}^t)
\tag{A.1}
\]

**解读**：这仅是公式 (1) 的记号扩展，上标 \(t\) 表示第 \(t\) 次训练迭代时的参数状态。\(\pmb{\theta}^t\) 是经过 \(t\) 步梯度下降后的参数值。

---

#### 公式 (A.2)：梯度下降参数更新规则（VGD）

\[
\pmb{\theta}^{t+1} = \pmb{\theta}^t - \eta\left.\frac{\partial\Pi}{\partial\pmb{\theta}}\right|_{\pmb{\theta}^t}
\tag{A.2}
\]

**数学事实**：这是**梯度下降法**（Gradient Descent）的参数更新公式。

- \(\eta > 0\) 是学习率（步长）
- \(\frac{\partial\Pi}{\partial\pmb{\theta}}\) 是势能泛函关于网络参数的梯度向量（通过链式法则和反向传播算法计算）
- 负号保证参数沿损失减小方向移动

**推导逻辑**：梯度下降法来源于 Cauchy（1847）提出的最速下降法（Steepest Descent），其收敛性在 \(\Pi\) 为光滑（梯度 Lipschitz 连续）凸函数时有理论保证。

---

#### 公式 (A.3)：参数增量

\[
\Delta\pmb{\theta}^{t+1} = \pmb{\theta}^{t+1} - \pmb{\theta}^t = -\eta\left.\frac{\partial\Pi}{\partial\pmb{\theta}}\right|_{\pmb{\theta}^t}
\tag{A.3}
\]

**推导**：直接由公式 (A.2) 两边减去 \(\pmb{\theta}^t\) 得到：

\[
\Delta\pmb{\theta}^{t+1} = \pmb{\theta}^{t+1} - \pmb{\theta}^t \stackrel{(A.2)}{=} -\eta\left.\frac{\partial\Pi}{\partial\pmb{\theta}}\right|_{\pmb{\theta}^t}
\]

**数学事实**：这里利用了基本的**向量减法**。注意 \(\Delta\pmb{\theta}^{t+1} = \mathcal{O}(\eta)\)，当学习率 \(\eta\) 较小时，参数增量是"小量"。

---

#### 公式 (A.4)：位移场的 Taylor 展开

\[
\pmb{u}^{t+1}(\pmb{x};\,\pmb{\theta}^{t+1}) = \pmb{F}(\pmb{x};\,\pmb{\theta}^t) + \frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\left(\Delta\pmb{\theta}^{t+1}\right) + O\!\left(\|\Delta\pmb{\theta}^{t+1}\|^2\right) \approx \pmb{F}(\pmb{x};\,\pmb{\theta}^t) + \frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\left(\Delta\pmb{\theta}^{t+1}\right)
\tag{A.4}
\]

**严格推导**

**数学事实**：**Taylor 定理**（多元版本）：若 \(\pmb{F}(\pmb{x};\cdot):\mathbb{R}^P \to \mathbb{R}^d\) 在 \(\pmb{\theta}^t\) 处连续可微，则：

\[
\pmb{F}(\pmb{x};\,\pmb{\theta}^t + \Delta\pmb{\theta}) = \pmb{F}(\pmb{x};\,\pmb{\theta}^t) + \mathbf{J}_{\pmb{\theta}}\pmb{F}\big|_{\pmb{\theta}^t} \cdot \Delta\pmb{\theta} + O(\|\Delta\pmb{\theta}\|^2)
\]

其中 \(\mathbf{J}_{\pmb{\theta}}\pmb{F} \in \mathbb{R}^{d\times P}\) 是 \(\pmb{F}\) 关于参数 \(\pmb{\theta}\) 的 Jacobian 矩阵：

\[
\left(\mathbf{J}_{\pmb{\theta}}\pmb{F}\right)_{ik} = \frac{\partial F_i}{\partial\theta_k}, \qquad i=1,\ldots,d;\; k=1,\ldots,P
\]

代入 \(\pmb{\theta}^{t+1} = \pmb{\theta}^t + \Delta\pmb{\theta}^{t+1}\)，略去高阶项：

\[
\pmb{u}^{t+1} = \pmb{F}(\pmb{x};\,\pmb{\theta}^{t+1}) \approx \pmb{F}(\pmb{x};\,\pmb{\theta}^t) + \mathbf{J}_{\pmb{\theta}}\pmb{F}\big|_{\pmb{\theta}^t} \cdot \Delta\pmb{\theta}^{t+1}
\]

**符号对应**：论文中 \(\frac{\partial\pmb{F}(\pmb{x};\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\) 即是 \(\mathbf{J}_{\pmb{\theta}}\pmb{F}\)（Jacobian 矩阵），写法等价。

**近似的有效条件**：当学习率 \(\eta\) 足够小，\(\|\Delta\pmb{\theta}^{t+1}\| = \eta\|\partial\Pi/\partial\pmb{\theta}\|\) 较小时，高阶项 \(O(\eta^2)\) 可忽略。

---

#### 公式 (A.5)：一步训练后的位移增量

\[
\Delta\pmb{u}^{t+1} = \pmb{u}^{t+1} - \pmb{u}^t \approx \frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\left(\Delta\pmb{\theta}^{t+1}\right)
\tag{A.5}
\]

**推导**

由公式 (A.1) 和公式 (A.4)：

\[
\Delta\pmb{u}^{t+1} = \pmb{u}^{t+1} - \pmb{u}^t = \pmb{F}(\pmb{x};\,\pmb{\theta}^{t+1}) - \pmb{F}(\pmb{x};\,\pmb{\theta}^t)
\stackrel{(A.4)}{\approx} \frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}(\Delta\pmb{\theta}^{t+1})
\]

**数学事实**：这里使用了**线性近似**（linearization），即用 Jacobian 矩阵代替函数在参数空间的非线性变化。这在数值分析中是一个基本技术，对应于一阶 Taylor 展开。

---

#### 公式 (A.6)：代入参数增量后的位移增量

\[
\Delta\pmb{u}^{t+1} \approx -\eta\,\frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\,\frac{\partial\Pi}{\partial\pmb{\theta}}
\tag{A.6}
\]

**推导**

将公式 (A.3) 代入公式 (A.5)：

\[
\Delta\pmb{u}^{t+1} \stackrel{(A.5)}{\approx} \frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\left(\Delta\pmb{\theta}^{t+1}\right)
\stackrel{(A.3)}{=} \frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\left(-\eta\frac{\partial\Pi}{\partial\pmb{\theta}}\right)
= -\eta\,\frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\,\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

**数学事实**：这里使用了**矩阵-向量乘法的线性性**：

\[
\mathbf{A}(c\,\pmb{b}) = c\,\mathbf{A}\pmb{b}
\]

其中 \(c = -\eta\) 是标量，\(\mathbf{A} = \frac{\partial\pmb{F}^\mathrm{T}}{\partial\pmb{\theta}}\) 是矩阵，\(\pmb{b} = \frac{\partial\Pi}{\partial\pmb{\theta}}\) 是向量。

**物理解读**：\(\frac{\partial\pmb{F}^\mathrm{T}}{\partial\pmb{\theta}}\) 是参数空间到位移空间的线性映射（"传播矩阵"），\(\frac{\partial\Pi}{\partial\pmb{\theta}}\) 是参数空间中的梯度向量，其乘积 \(\frac{\partial\pmb{F}^\mathrm{T}}{\partial\pmb{\theta}}\frac{\partial\Pi}{\partial\pmb{\theta}}\) 是这个梯度在位移空间中的"投影"，表示当参数沿最速下降方向变化时位移场的变化方向和量级。

---

#### 公式 (A.7)：连续极限下的伪速度

\[
\frac{\partial\pmb{u}}{\partial t} = \lim_{\eta\to 0}\frac{\Delta\pmb{u}^{t+1}}{\eta} = -\left.\frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\,\frac{\partial\Pi}{\partial\pmb{\theta}}\right|_{\pmb{\theta}^t}
\tag{A.7}
\]

这就是正文中公式 (11) 的等价形式。

**严格推导**

由公式 (A.6)：

\[
\frac{\Delta\pmb{u}^{t+1}}{\eta} \approx -\frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\,\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

注意右端与 \(\eta\) 无关（在 \(\eta \to 0\) 的极限下，高阶项 \(O(\eta)/\eta = O(1) \to 0\)）。因此：

\[
\frac{\partial\pmb{u}}{\partial t} := \lim_{\eta\to 0}\frac{\Delta\pmb{u}^{t+1}}{\eta} = -\frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta}^t)^\mathrm{T}}{\partial\pmb{\theta}}\,\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

**数学事实**：这里使用了**导数的定义**（离散差商的极限 = 连续导数），将离散的训练步数 \(t\) 视为连续"伪时间"参数。同时，这与**梯度流方程**（Gradient Flow Equation）密切相关：

\[
\frac{\mathrm{d}\pmb{\theta}}{\mathrm{d}t} = -\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

（参见公式 A.8）。将此代入链式法则：

\[
\frac{\partial\pmb{u}}{\partial t} = \frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta})^\mathrm{T}}{\partial\pmb{\theta}}\,\frac{\mathrm{d}\pmb{\theta}}{\mathrm{d}t} = -\frac{\partial\pmb{F}(\pmb{x};\,\pmb{\theta})^\mathrm{T}}{\partial\pmb{\theta}}\,\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

与公式 (A.7) 完全一致，**验证了推导的自洽性**。

**伪速度的物理图像**：公式 (A.7) 揭示，PINN 训练过程中位移场以速度 \(\partial\pmb{u}/\partial t\) "流动"，方向由势能在参数空间的梯度决定，大小由 Jacobian 矩阵（网络对参数的敏感性）调制。这与粘性流体中"势能最速下降"的耗散动力学完全类比。

---

#### 公式 (A.8)：参数的梯度流方程

\[
\frac{\partial\pmb{\theta}}{\partial t} \approx -\frac{\partial\Pi}{\partial\pmb{\theta}}
\tag{A.8}
\]

**推导**

由公式 (A.3) 在 \(\eta \to 0\) 极限下：

\[
\frac{\mathrm{d}\pmb{\theta}}{\mathrm{d}t} = \lim_{\eta\to 0}\frac{\Delta\pmb{\theta}^{t+1}}{\eta} = \lim_{\eta\to 0}\frac{-\eta\,\frac{\partial\Pi}{\partial\pmb{\theta}}}{\eta} = -\frac{\partial\Pi}{\partial\pmb{\theta}}
\]

**数学事实**：公式 (A.8) 是经典的**梯度流**（Gradient Flow）常微分方程，在优化理论和机器学习理论中有广泛研究。其解沿势能的负梯度方向演化，收敛到 \(\Pi\) 的驻点（若 \(\Pi\) 满足 KL（Kurdyka-Łojasiewicz）条件，则收敛到局部极小值）。

**与梯度下降的对应**：离散梯度下降（公式 A.2）是梯度流（公式 A.8）的显式 Euler 离散，步长 \(\eta\) 对应时间步长 \(\Delta t\)。这与常微分方程数值解的稳定性条件相关：步长 \(\eta\) 不能太大（否则数值不稳定），这在接触问题中具体体现为不能让位移增量超过接触间距。

---

## 补充推导：Neo-Hookean 应变能密度 {#neohookean}

本文大量数值算例采用 **Neo-Hookean 超弹性材料模型**，其应变能密度函数如代码中所示：

\[
\Psi(\pmb{F}_{\rm def}) = \frac{\lambda}{4}(J^2-1) - \left(\frac{\lambda}{2}+\mu\right)\ln J + \frac{\mu}{2}(I_1-2)
\]

**符号定义**

| 符号 | 定义 | 含义 |
|:---|:---|:---|
| \(\pmb{F}_{\rm def}\) | 变形梯度张量 | 映射参考构型到当前构型 |
| \(J = \det(\pmb{F}_{\rm def})\) | Jacobian 行列式 | 体积比（变形前后），须有 \(J > 0\) |
| \(\pmb{C} = \pmb{F}_{\rm def}^\mathrm{T}\pmb{F}_{\rm def}\) | 右 Cauchy-Green 变形张量 | |
| \(I_1 = \mathrm{tr}(\pmb{C}) = \|\pmb{F}_{\rm def}\|_F^2\) | 第一主不变量 | 形变大小的度量 |
| \(\lambda = \dfrac{E\nu}{(1+\nu)(1-2\nu)}\) | 第一 Lamé 常数 | 体积压缩相关 |
| \(\mu = \dfrac{E}{2(1+\nu)}\) | 第二 Lamé 常数（剪切模量） | 剪切变形相关 |

**Lamé 常数的推导（从 \(E, \nu\) 出发）**

给定杨氏模量 \(E\) 和泊松比 \(\nu\)，Lamé 常数由以下关系定义：

\[
\lambda = \frac{E\nu}{(1+\nu)(1-2\nu)}, \qquad \mu = \frac{E}{2(1+\nu)}
\]

验证（代码中的计算）：

```python
la1 = mu_poisson / (1 + mu_poisson) / (1 - 2*mu_poisson) * E1
# = ν/((1+ν)(1-2ν)) * E = λ  ✓

nu1 = 1 / (1 + mu_poisson) / 2 * E1
# = 1/((1+ν)*2) * E = E/(2(1+ν)) = μ  ✓
```

**应变能密度的来源（Ciarlet Neo-Hookean 模型）**

Neo-Hookean 模型是线弹性 Hooke 定律在有限变形下的自然推广，由以下思路导出：

**步骤1**：对于无穷小变形（\(\pmb{F}_{\rm def} = \pmb{I} + \pmb{H}\)，\(\|\pmb{H}\| \ll 1\)），Neo-Hookean 应退化为线弹性。线弹性的应变能密度为：

\[
\Psi_{\rm lin} = \frac{\lambda}{2}(\mathrm{tr}\,\pmb{\varepsilon})^2 + \mu\,\pmb{\varepsilon}:\pmb{\varepsilon}
\]

其中线应变 \(\pmb{\varepsilon} = \frac{1}{2}(\pmb{H}+\pmb{H}^\mathrm{T})\)。

**步骤2**：用有限变形的不变量（\(J, I_1\)）替换小应变量：

在线弹性极限下：
- \(J = \det(\pmb{I}+\pmb{H}) \approx 1 + \mathrm{tr}(\pmb{H}) = 1 + \mathrm{tr}(\pmb{\varepsilon})\)，故 \(\mathrm{tr}(\pmb{\varepsilon}) \approx J - 1\)
- \(I_1 = \|\pmb{F}_{\rm def}\|^2 \approx d + 2\mathrm{tr}(\pmb{\varepsilon})\)（\(d\) 为空间维数），故 \(I_1 - d \approx 2\mathrm{tr}(\pmb{\varepsilon})\)

**步骤3**：构造满足客观性（材料框架无差异原理）的超弹性势，要求：
- \(J > 0\) 时有意义（体积不坍缩）
- \(J = 1, I_1 = d\) 时（无变形）\(\Psi = 0\)
- 对 \(J \to 0^+\) 时 \(\Psi \to +\infty\)（强体积压缩需要无限大能量）

由此得到 Ciarlet（1988）提出的 Neo-Hookean 模型：

\[
\Psi = \frac{\lambda}{4}(J^2-1) - \left(\frac{\lambda}{2}+\mu\right)\ln J + \frac{\mu}{2}(I_1-2)
\tag{NH}
\]

**各项的物理意义**：
- \(\frac{\lambda}{4}(J^2-1)\)：**体积压缩能**，当 \(J>1\)（膨胀）或 \(J<1\)（压缩）时增大
- \(-\left(\frac{\lambda}{2}+\mu\right)\ln J\)：**体积约束能**，确保 \(J\to 0^+\) 时 \(\Psi\to+\infty\)（因 \(-\ln J\to+\infty\)），\(J=1\) 时为零
- \(\frac{\mu}{2}(I_1-2)\)：**等体积剪切能**（distortional energy），当材料发生形状改变但体积不变时增大

**验证小变形极限**：令 \(\pmb{F}_{\rm def} = \pmb{I}+\pmb{H}\)，\(\|\pmb{H}\|\to 0\)：
- \(J \approx 1+\mathrm{tr}(\pmb{H})\)，\(\ln J \approx \mathrm{tr}(\pmb{H})-\frac{1}{2}(\mathrm{tr}(\pmb{H}))^2\)
- \(J^2 \approx 1+2\mathrm{tr}(\pmb{H})+(\mathrm{tr}(\pmb{H}))^2\)
- \(I_1 \approx 2+2\mathrm{tr}(\pmb{\varepsilon})\)

代入 (NH) 并保留至二阶项，可验证 \(\Psi \approx \frac{\lambda}{2}(\mathrm{tr}\,\pmb{\varepsilon})^2 + \mu\,\|\pmb{\varepsilon}\|_F^2\)，与线弹性吻合。

**代码实现**（`PINN.py`）：

```python
# 变形梯度分量（通过自动微分得到 ∂u/∂x, ∂u/∂y 等）
F11 = 1 + U_x;   F12 = U_y
F21 = V_x;       F22 = 1 + V_y

# J = det(F)
J = F11 * F22 - F12 * F21

# I1 = tr(F^T F) = F11^2 + F12^2 + F21^2 + F22^2
I1 = F11**2 + F12**2 + F21**2 + F22**2

# Neo-Hookean 应变能密度 Ψ
Ein = 0.25*la*(J**2-1) - (la/2+nu)*tf.math.log(J) + 0.5*nu*(I1-2)
```

与公式 (NH) 逐项对应：
- `0.25*la*(J**2-1)` → \(\frac{\lambda}{4}(J^2-1)\) ✓
- `-(la/2+nu)*tf.math.log(J)` → \(-\left(\frac{\lambda}{2}+\mu\right)\ln J\) ✓
- `0.5*nu*(I1-2)` → \(\frac{\mu}{2}(I_1-2)\) ✓

---

## 总结：全部公式编号索引

| 公式编号 | 内容简述 | 所在节 |
|:---|:---|:---|
| (1) | 神经网络近似位移场 | §2.1 |
| (2) | 最小势能原理 | §2.1 |
| (3) | 势能泛函 = 应变能 − 外力势能 | §2.1 |
| (4) | 应变能（体积分） | §2.1 |
| (5) | 外力势能（边界积分） | §2.1 |
| (6) | 本质边界条件 (Dirichlet BC) | §2.2 |
| (7) | 软边界罚函数 \(\Pi_{\rm EBC}\) | §2.2 |
| (8) | 修正势能泛函 \(\Pi^*\) | §2.2 |
| (9) | 硬边界条件网络结构 | §2.2 |
| (10) | 距离函数（到本质边界） | §2.2 |
| (11) | 训练的伪速度 | §2.3 |
| (12) | 位移增量与伪速度的关系 | §2.3 |
| (13) | 含接触的总势能 | §3.1 |
| (14) | 接触势能双面积分 | §3.1 |
| (15) | 指数型接触势函数 | §3.1 |
| (18) | 接触点距离矩阵 | §3.2 |
| (19) | 坐标差矩阵（外积公式） | §3.2 |
| (20) | 接触势矩阵 | §3.2 |
| (21) | 渐进加载边界条件泛函 | §3.3 |
| (22) | 含缩放的硬边界网络输出 | §3.3 |
| (23) | Hertz 接触网络输出 | §3.4 |
| (24) | 橡皮熨烫网络输出 | §4.1 |
| (A.1) | 第 \(t\) 步位移预测 | 附录A |
| (A.2) | VGD 参数更新 | 附录A |
| (A.3) | 参数增量 | 附录A |
| (A.4) | Taylor 展开（一阶近似） | 附录A |
| (A.5) | 位移场增量 | 附录A |
| (A.6) | 代入参数增量后的位移增量 | 附录A |
| (A.7) | 伪速度（连续极限） | 附录A |
| (A.8) | 梯度流方程 | 附录A |
| (NH) | Neo-Hookean 应变能密度 | 补充推导 |

